---
phase: 01-domain-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/contract-extraction/domain.yaml
  - skills/contract-extraction/SKILL.md
  - skills/contract-extraction/references/entity-types.md
  - skills/contract-extraction/references/relation-types.md
autonomous: true
requirements: [DCFG-02, DCFG-03]

must_haves:
  truths:
    - "Contract domain.yaml defines exactly 7 entity types: PARTY, OBLIGATION, DEADLINE, COST, CLAUSE, SERVICE, VENUE"
    - "Contract domain.yaml defines exactly 7 relation types: OBLIGATES, CONFLICTS_WITH, DEPENDS_ON, COSTS, PROVIDES, RESTRICTS, RELATED_TO"
    - "Every relation source_type and target_type references a defined entity type"
    - "Contract SKILL.md provides contract-specific extraction guidance for Claude agents"
    - "sift-kg load_domain() successfully loads the contract domain.yaml without errors"
  artifacts:
    - path: "skills/contract-extraction/domain.yaml"
      provides: "Contract domain schema with 7 entity types and 7 relation types"
      contains: "name: \"Contract Analysis\""
    - path: "skills/contract-extraction/SKILL.md"
      provides: "Contract extraction prompt for Claude agents"
      contains: "Contract"
    - path: "skills/contract-extraction/references/entity-types.md"
      provides: "Documentation of contract entity types"
    - path: "skills/contract-extraction/references/relation-types.md"
      provides: "Documentation of contract relation types"
  key_links:
    - from: "skills/contract-extraction/domain.yaml"
      to: "sift-kg DomainConfig"
      via: "load_domain() Pydantic validation"
      pattern: "load_domain.*domain_path"
---

<objective>
Create the complete contract domain package: domain.yaml with the 7 entity types and 7 relation types (6 + RELATED_TO fallback), SKILL.md extraction prompt, and references/ documentation. This is a content creation task -- no code changes.

Purpose: Provides the contract ontology that extraction agents will use to process 62+ Sample 2026 event contracts.
Output: Complete `skills/contract-extraction/` directory satisfying D-03 package requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-configuration/01-CONTEXT.md
@.planning/phases/01-domain-configuration/01-RESEARCH.md

<interfaces>
<!-- Reference implementation: biomedical domain structure -->
From skills/drug-discovery-extraction/domain.yaml:
```yaml
name: "Drug Discovery"
version: "1.0.0"
description: |
  Domain for extracting structured knowledge graphs from drug discovery...
system_context: |
  You are analyzing drug discovery...
fallback_relation: RELATED_TO  # (implicit, not set in biomedical but supported by DomainConfig)
entity_types:
  COMPOUND:
    description: "..."
    extraction_hints: ["..."]
relation_types:
  TARGETS:
    description: "..."
    source_types: [COMPOUND]
    target_types: [GENE, PROTEIN]
    extraction_hints: ["..."]
```

<!-- sift-kg DomainConfig Pydantic model expects -->
```python
# DomainConfig fields:
#   name: str (required)
#   version: str (required)
#   description: str (required)
#   system_context: str | None
#   fallback_relation: str | None
#   entity_types: dict[str, EntityTypeConfig]
#     EntityTypeConfig: description (str), extraction_hints (list[str])
#   relation_types: dict[str, RelationTypeConfig]
#     RelationTypeConfig: description (str), source_types (list[str]), target_types (list[str]),
#       symmetric (bool, optional), review_required (bool, optional), extraction_hints (list[str])
#   schema_free: bool (default False)
```

From skills/drug-discovery-extraction/SKILL.md (structure reference):
- Title section with domain name
- Extraction instructions for Claude agents
- Entity type descriptions with examples
- Relation type descriptions with examples
- Output format guidance
- Confidence calibration guide
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contract domain.yaml</name>
  <files>skills/contract-extraction/domain.yaml</files>
  <read_first>
    - skills/drug-discovery-extraction/domain.yaml (FULL file -- reference implementation for exact YAML structure, field names, nesting)
    - .planning/phases/01-domain-configuration/01-RESEARCH.md (contract domain.yaml example starting at "### Contract domain.yaml" section, lines 311-465)
  </read_first>
  <action>
Create `skills/contract-extraction/domain.yaml` using the EXACT content from the research document (01-RESEARCH.md lines 311-465). This YAML has been verified against sift-kg's DomainConfig Pydantic model.

Key fields (per D-07, D-08, D-09, D-10, D-11, D-12):
- `name: "Contract Analysis"`
- `version: "1.0.0"`
- `description:` multi-line describing contract/vendor agreement extraction for Sample 2026
- `system_context:` multi-line with naming standards, disambiguation rules (D-12: OBLIGATION vs CLAUSE, COST vs PENALTY, SERVICE vs VENUE), confidence calibration (D-11: 0.9+ explicit, 0.7-0.89 implied, 0.5-0.69 inferred)
- `fallback_relation: RELATED_TO` (per D-10)
- 7 entity types (D-07): PARTY, OBLIGATION, DEADLINE, COST, CLAUSE, SERVICE, VENUE -- each with `description` and `extraction_hints` list (D-09)
- 7 relation types (D-08 + D-10): OBLIGATES, CONFLICTS_WITH, DEPENDS_ON, COSTS, PROVIDES, RESTRICTS, RELATED_TO -- each with `description`, `source_types`, `target_types`, `extraction_hints`. CONFLICTS_WITH and RELATED_TO have `symmetric: true`. CONFLICTS_WITH has `review_required: true`.

Verify all relation source_types and target_types reference only defined entity types (PARTY, OBLIGATION, DEADLINE, COST, CLAUSE, SERVICE, VENUE).

Create the directory first: `skills/contract-extraction/`
  </action>
  <verify>
    <automated>python -c "import yaml; from pathlib import Path; d = yaml.safe_load(Path('skills/contract-extraction/domain.yaml').read_text()); et = set(d['entity_types'].keys()); assert len(et) == 7, f'Expected 7 entity types, got {len(et)}'; rt = set(d['relation_types'].keys()); assert len(rt) == 7, f'Expected 7 relation types, got {len(rt)}'; assert d['fallback_relation'] == 'RELATED_TO'; [assert s in et for r in d['relation_types'].values() for s in r.get('source_types', [])]; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - skills/contract-extraction/domain.yaml exists
    - File contains `name: "Contract Analysis"`
    - File contains `version: "1.0.0"`
    - File contains `fallback_relation: RELATED_TO`
    - File contains entity types: PARTY, OBLIGATION, DEADLINE, COST, CLAUSE, SERVICE, VENUE (exactly 7)
    - File contains relation types: OBLIGATES, CONFLICTS_WITH, DEPENDS_ON, COSTS, PROVIDES, RESTRICTS, RELATED_TO (exactly 7)
    - CONFLICTS_WITH has `symmetric: true` and `review_required: true`
    - RELATED_TO has `symmetric: true`
    - All relation source_types and target_types reference only defined entity type names
    - YAML parses without error: `python -c "import yaml; yaml.safe_load(open('skills/contract-extraction/domain.yaml'))"`
  </acceptance_criteria>
  <done>Contract domain.yaml exists with 7 entity types, 7 relation types, all cross-references valid, YAML parseable</done>
</task>

<task type="auto">
  <name>Task 2: Create contract SKILL.md and references/</name>
  <files>
    skills/contract-extraction/SKILL.md
    skills/contract-extraction/references/entity-types.md
    skills/contract-extraction/references/relation-types.md
  </files>
  <read_first>
    - skills/drug-discovery-extraction/SKILL.md (reference implementation for structure, tone, level of detail)
    - skills/drug-discovery-extraction/references/entity-types.md (reference for entity type documentation format)
    - skills/drug-discovery-extraction/references/relation-types.md (reference for relation type documentation format)
    - skills/contract-extraction/domain.yaml (just created in Task 1 -- source of truth for entity/relation names and descriptions)
  </read_first>
  <action>
Create 3 files per D-03 (domain package = domain.yaml + SKILL.md + references/).

**`skills/contract-extraction/SKILL.md`** (per D-03, D-09, D-17):
Follow the biomedical SKILL.md structure but with contract-specific content:
- Title: `# Contract Analysis Extraction Skill`
- Overview: extracting knowledge from event contracts, vendor agreements, and service-level agreements for Sample 2026
- Entity types section: list all 7 with descriptions and extraction guidance matching domain.yaml extraction_hints
- Relation types section: list all 7 with descriptions, source/target constraints, and extraction guidance
- Output format section: same JSON format as biomedical (entities with name/entity_type/attributes/confidence/context, relations with relation_type/source_entity/target_entity/confidence/evidence)
- Use `entity_type` and `relation_type` field names (CRITICAL -- same as biomedical SKILL.md warning)
- Confidence calibration section per D-11 (0.9+ for shall/must, 0.7-0.89 for implied, 0.5-0.69 for inferred)
- Disambiguation rules per D-12 (OBLIGATION vs CLAUSE, COST vs PENALTY, SERVICE vs VENUE)
- Naming standards: use formal party names, action verbs for obligations, include dates in deadlines, include amounts in costs
- Write extraction command: same `echo '<json>' | python3 ${CLAUDE_PLUGIN_ROOT}/scripts/build_extraction.py <doc_id> <output_dir>` pattern

**`skills/contract-extraction/references/entity-types.md`**:
- Document each of the 7 entity types with:
  - Description (from domain.yaml)
  - Attributes to capture (e.g., PARTY: role, legal_name; COST: amount, currency, payment_terms; DEADLINE: date, what_is_due)
  - Examples from event contracts (e.g., PARTY: "Aramark Sports & Entertainment Services, LLC", VENUE: "Hall A, Pennsylvania Convention Center")

**`skills/contract-extraction/references/relation-types.md`**:
- Document each of the 7 relation types with:
  - Description (from domain.yaml)
  - Source and target entity type constraints
  - Example triples (e.g., "Aramark OBLIGATES Provide catering for 500 guests")

Create `references/` directory before writing files.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; s = Path('skills/contract-extraction/SKILL.md'); r1 = Path('skills/contract-extraction/references/entity-types.md'); r2 = Path('skills/contract-extraction/references/relation-types.md'); assert s.exists(), 'SKILL.md missing'; assert r1.exists(), 'entity-types.md missing'; assert r2.exists(), 'relation-types.md missing'; c = s.read_text(); assert 'entity_type' in c, 'SKILL.md missing entity_type'; assert 'PARTY' in c; assert 'OBLIGATION' in c; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - skills/contract-extraction/SKILL.md exists and is > 200 lines
    - skills/contract-extraction/SKILL.md contains "Contract Analysis" or "contract"
    - skills/contract-extraction/SKILL.md contains all 7 entity type names: PARTY, OBLIGATION, DEADLINE, COST, CLAUSE, SERVICE, VENUE
    - skills/contract-extraction/SKILL.md contains all 7 relation type names: OBLIGATES, CONFLICTS_WITH, DEPENDS_ON, COSTS, PROVIDES, RESTRICTS, RELATED_TO
    - skills/contract-extraction/SKILL.md contains `entity_type` (not just `type`)
    - skills/contract-extraction/SKILL.md contains `build_extraction.py`
    - skills/contract-extraction/references/ directory exists
    - skills/contract-extraction/references/entity-types.md exists and is > 50 lines
    - skills/contract-extraction/references/relation-types.md exists and is > 50 lines
    - No validation-scripts/ directory exists (per D-18, contract domain has none initially)
  </acceptance_criteria>
  <done>Contract SKILL.md + references/ exist, contain all entity/relation types, follow biomedical structure pattern</done>
</task>

</tasks>

<verification>
- `python -c "import yaml; yaml.safe_load(open('skills/contract-extraction/domain.yaml'))"` -- YAML valid
- `python -c "from sift_kg import load_domain; from pathlib import Path; d = load_domain(domain_path=Path('skills/contract-extraction/domain.yaml')); print(d.name)"` -- prints "Contract Analysis" (sift-kg validates structure)
- All 3 required files exist per D-03: domain.yaml, SKILL.md, references/ directory
- `ruff check skills/` passes (no Python files in this plan, but good hygiene)
</verification>

<success_criteria>
- Complete contract domain package at skills/contract-extraction/ with domain.yaml + SKILL.md + references/
- domain.yaml has 7 entity types and 7 relation types with valid cross-references
- sift-kg load_domain() loads it without error
- SKILL.md provides extraction guidance matching domain.yaml schema
- No validation-scripts/ directory (per D-18)
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-configuration/01-02-SUMMARY.md`
</output>
