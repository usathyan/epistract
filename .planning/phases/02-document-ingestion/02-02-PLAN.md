---
phase: 02-document-ingestion
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/ingest_documents.py
  - tests/test_unit.py
autonomous: true
requirements: [INGS-01, INGS-02, INGS-03, INGS-04, INGS-08]

must_haves:
  truths:
    - "PDF files are ingested from a user-provided local corpus path via CLI"
    - "XLS and EML files are parsed through the same Kreuzberg pipeline as PDFs"
    - "Per-document metadata is captured in triage.json with all required fields"
    - "Documents are classified as text-native, scanned, or mixed"
    - "Parse failures are logged in triage.json and skipped without crashing the pipeline"
    - "Progress bar shows file count and current filename during ingestion"
  artifacts:
    - path: "scripts/ingest_documents.py"
      provides: "Complete ingestion pipeline with CLI, triage, error handling"
      contains: "if __name__"
    - path: "tests/test_unit.py"
      provides: "Integration tests for triage output, error handling, CLI"
  key_links:
    - from: "scripts/ingest_documents.py"
      to: "triage.json"
      via: "json.dumps with indent=2"
      pattern: "triage.json"
    - from: "scripts/ingest_documents.py"
      to: "ingested/<doc_id>.txt"
      via: "Path write_text"
      pattern: 'ingested.*\\.txt'
---

<objective>
Harden the ingestion pipeline with end-to-end integration tests covering triage output, multi-format parsing (PDF/XLS/EML), error handling for corrupt files, and CLI validation. Ensure the pipeline produces correct triage.json with all D-08 metadata fields.

Purpose: Validates that the ingestion module works as a complete system -- from CLI invocation through corpus scanning, parsing, error handling, to triage.json output. Catches integration issues before Phase 3 depends on the output.

Output: Updated `scripts/ingest_documents.py` (if fixes needed), comprehensive integration tests in `tests/test_unit.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-document-ingestion/02-CONTEXT.md
@.planning/phases/02-document-ingestion/02-01-SUMMARY.md
@scripts/ingest_documents.py
@tests/test_unit.py

<interfaces>
<!-- Expected from Plan 01 — functions the executor needs -->
From scripts/ingest_documents.py (created in Plan 01):
```python
def discover_corpus(corpus_path: Path) -> list[Path]
def sanitize_doc_id(filename: str) -> str
def detect_category(file_path: Path, corpus_root: Path) -> str
def parse_document(file_path: Path) -> str | dict
def compute_readiness_score(text: str, file_size: int) -> float
def classify_parse_type(text: str, file_size: int) -> str
def build_document_metadata(file_path: Path, corpus_root: Path, text: str | dict, doc_id: str) -> dict
def ingest_corpus(corpus_path: Path, output_dir: Path, domain_name: str | None = None) -> dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration tests for triage output and error handling</name>
  <files>tests/test_unit.py, scripts/ingest_documents.py</files>
  <read_first>
    - tests/test_unit.py (current state after Plan 01 -- see existing test patterns and ingest tests)
    - scripts/ingest_documents.py (current implementation from Plan 01)
    - .planning/phases/02-document-ingestion/02-CONTEXT.md (D-07, D-08 triage field specs)
  </read_first>
  <behavior>
    - Test: ingest_corpus on fixtures dir produces triage.json with keys: ingested_at, corpus_path, domain, total_files, successful, failed, documents
    - Test: each document entry in triage.json has all D-08 fields: doc_id, filename, file_path, file_size_bytes, page_count, category, parse_type, text_length, parse_errors, extraction_readiness_score
    - Test: parse_type values are one of "text", "scanned", "mixed", "failed"
    - Test: extraction_readiness_score is float between 0.0 and 1.0
    - Test: ingesting a directory with a non-parseable file (e.g., .zip or corrupt) logs error in triage.json but pipeline completes
    - Test: ingesting an empty directory produces triage.json with total_files=0
    - Test: ingest_corpus creates ingested/ subdirectory with .txt files for successful parses
    - Test: txt file content matches parse_document output for that fixture
  </behavior>
  <action>
    **Add integration tests to `tests/test_unit.py`:**

    Create a new section after the unit tests from Plan 01 with separator comment. Add tests prefixed `test_ingest_integration_`:

    1. `test_ingest_integration_triage_json_structure` — Create a temp dir with a mock corpus structure:
       ```
       tmpdir/corpus/Hotel/contract_a.pdf -> copy sample_contract_a.pdf
       tmpdir/corpus/AV/contract_b.pdf -> copy sample_contract_b.pdf
       ```
       Call `ingest_corpus(corpus_path, output_path)`. Load `output_path / "triage.json"`. Assert top-level keys exist. Assert `total_files >= 2`. Assert `successful + failed == total_files`.

    2. `test_ingest_integration_document_metadata_fields` — From same triage.json, check each document entry has all 10 D-08 fields. Assert `doc_id` is a string, `file_size_bytes` is int > 0, `category` is in `{"hotel", "av", "uncategorized"}`, `parse_type` is in `{"text", "scanned", "mixed", "failed"}`, `extraction_readiness_score` is float and `0.0 <= score <= 1.0`.

    3. `test_ingest_integration_error_handling` — Create a corrupt file (write random bytes to `tmpdir/corpus/Security/corrupt.pdf`). Call `ingest_corpus`. Assert pipeline completes (no exception). Assert triage.json has entry for corrupt.pdf with `parse_errors` not None or `parse_type == "failed"`.

    4. `test_ingest_integration_empty_corpus` — Empty directory. Assert triage.json has `total_files == 0` and `documents == []`.

    5. `test_ingest_integration_text_files_written` — After ingest_corpus, assert `output_path / "ingested"` dir exists. Assert at least one `.txt` file exists in it. Read one and assert len > 0.

    6. `test_ingest_integration_txt_content_matches` — Parse a known fixture directly with `parse_document`, then check the corresponding `.txt` file in `ingested/` has the same content.

    **Fix any issues found in `scripts/ingest_documents.py`** during test development. Common fixes:
    - Ensure `ingest_corpus` creates output_dir if it doesn't exist (`output_dir.mkdir(parents=True, exist_ok=True)`)
    - Ensure parse errors don't crash the progress bar
    - Ensure triage.json includes all D-08 fields even for failed documents

    Mark tests with `@pytest.mark.skipif(not HAS_INGEST, reason="ingest_documents not available")`.

    Use `tempfile.mkdtemp()` for test directories. Clean up with `shutil.rmtree()` in try/finally or use `tmp_path` pytest fixture.
  </action>
  <verify>
    <automated>cd /Users/umeshbhatt/code/epistract && python -m pytest tests/test_unit.py -v -k "test_ingest" --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_ingest_integration_" tests/test_unit.py` returns at least 5
    - `grep "triage.json" tests/test_unit.py` finds at least 3 matches
    - `grep "extraction_readiness_score" tests/test_unit.py` finds at least 1 match
    - `grep "parse_errors" tests/test_unit.py` finds at least 1 match
    - `grep "parse_type" tests/test_unit.py` finds at least 1 match
    - `python -m pytest tests/test_unit.py -v -k "test_ingest_integration" --no-header 2>&1 | grep -c "PASSED"` is >= 5
    - All existing tests still pass: `python -m pytest tests/test_unit.py -v --no-header 2>&1 | grep "failed"` shows 0 failures
  </acceptance_criteria>
  <done>6+ integration tests pass covering triage.json structure, all D-08 metadata fields, error handling, empty corpus, text file output, and content fidelity</done>
</task>

<task type="auto">
  <name>Task 2: Validate CLI entry point and end-to-end run</name>
  <files>scripts/ingest_documents.py</files>
  <read_first>
    - scripts/ingest_documents.py (verify __main__ block, CLI arg parsing)
    - scripts/run_sift.py (CLI pattern: sys.argv parsing, help output)
  </read_first>
  <action>
    **Verify and fix the CLI entry point in `scripts/ingest_documents.py`:**

    1. Ensure the `__main__` block handles these cases correctly:
       - No args: prints module docstring and exits with code 1
       - `--help` flag: prints module docstring and exits with code 0
       - Missing `--output` flag: defaults to `Path("./epistract-output")`
       - Corpus path that doesn't exist: prints error message `"Error: corpus path does not exist: {path}"` and exits with code 1
       - `--domain` flag: passes domain name to `ingest_corpus`

    2. Ensure `ingest_corpus` handles edge cases:
       - corpus_path is a file, not directory: return error dict `{"error": "corpus_path is not a directory"}`
       - output_dir doesn't exist: create it with `output_dir.mkdir(parents=True, exist_ok=True)`
       - Rich import failure: fallback to plain print (wrap Rich usage in try/except)

    3. Run `ruff check scripts/ingest_documents.py` and `ruff format scripts/ingest_documents.py` to ensure code quality.

    4. Run the CLI end-to-end with test fixtures:
       ```bash
       cd /Users/umeshbhatt/code/epistract
       python scripts/ingest_documents.py tests/fixtures --output /tmp/test-ingest-output
       cat /tmp/test-ingest-output/triage.json
       ls /tmp/test-ingest-output/ingested/
       ```
       Verify triage.json is valid JSON with expected structure.
  </action>
  <verify>
    <automated>cd /Users/umeshbhatt/code/epistract && python scripts/ingest_documents.py tests/fixtures --output /tmp/test-ingest-e2e 2>&1 && python -c "import json; t=json.load(open('/tmp/test-ingest-e2e/triage.json')); assert t['total_files']>=2, f'Expected >=2 files, got {t[\"total_files\"]}'; assert t['successful']>=1, f'Expected >=1 successful'; print(f'OK: {t[\"total_files\"]} files, {t[\"successful\"]} successful, {t[\"failed\"]} failed')"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/ingest_documents.py 2>&1; echo $?` outputs the docstring and exit code 1
    - `python scripts/ingest_documents.py /nonexistent/path 2>&1` outputs "Error: corpus path does not exist"
    - `python scripts/ingest_documents.py tests/fixtures --output /tmp/test-ingest-e2e` exits with code 0
    - `/tmp/test-ingest-e2e/triage.json` exists and is valid JSON
    - `python -c "import json; t=json.load(open('/tmp/test-ingest-e2e/triage.json')); print(t['total_files'])"` outputs a number >= 2
    - `ls /tmp/test-ingest-e2e/ingested/*.txt 2>/dev/null | wc -l` outputs at least 1
    - `ruff check scripts/ingest_documents.py` exits cleanly (no errors)
  </acceptance_criteria>
  <done>CLI entry point handles all edge cases, end-to-end run produces valid triage.json and text files, ruff passes</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_unit.py -v` — all tests pass
2. `python scripts/ingest_documents.py tests/fixtures --output /tmp/verify-ingest` — produces triage.json + ingested/ dir
3. `ruff check scripts/ingest_documents.py tests/test_unit.py` — no errors
4. `python -c "import json; t=json.load(open('/tmp/verify-ingest/triage.json')); [print(d['doc_id'], d['category'], d['parse_type'], d['extraction_readiness_score']) for d in t['documents']]"` — prints metadata for each doc
</verification>

<success_criteria>
- 6+ integration tests passing in test_unit.py
- CLI handles no-args, --help, bad path, --output, --domain flags
- End-to-end run on test fixtures produces valid triage.json with all D-08 fields
- Text files written to ingested/ directory
- Parse failures logged but don't crash pipeline
- ruff check passes on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/02-document-ingestion/02-02-SUMMARY.md`
</output>
