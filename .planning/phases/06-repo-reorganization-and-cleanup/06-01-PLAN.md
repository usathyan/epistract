---
phase: 06-repo-reorganization-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/__init__.py
  - core/domain_resolver.py
  - core/build_extraction.py
  - core/run_sift.py
  - core/ingest_documents.py
  - core/entity_resolution.py
  - core/chunk_document.py
  - core/label_communities.py
  - core/label_epistemic.py
  - domains/drug-discovery/domain.yaml
  - domains/drug-discovery/SKILL.md
  - domains/drug-discovery/references/entity-types.md
  - domains/drug-discovery/references/relation-types.md
  - domains/drug-discovery/validation/scan_patterns.py
  - domains/drug-discovery/validation/validate_smiles.py
  - domains/drug-discovery/validation/validate_sequences.py
  - domains/drug-discovery/epistemic.py
  - domains/contracts/domain.yaml
  - domains/contracts/SKILL.md
  - domains/contracts/references/entity-types.md
  - domains/contracts/references/relation-types.md
  - domains/contracts/epistemic.py
  - domains/contracts/extract.py
  - examples/workbench/__init__.py
  - examples/workbench/server.py
  - examples/workbench/api_chat.py
  - examples/workbench/api_graph.py
  - examples/workbench/api_sources.py
  - examples/workbench/data_loader.py
  - examples/workbench/system_prompt.py
  - examples/workbench/static/
  - scripts/launch_workbench.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03

must_haves:
  truths:
    - "Repository has core/, domains/, and examples/ top-level directories"
    - "Core scripts are in core/ with __init__.py package marker"
    - "Drug-discovery domain is self-contained in domains/drug-discovery/"
    - "Contract domain is self-contained in domains/contracts/"
    - "Workbench app is in examples/workbench/"
    - "Domain-specific Python (epistemic_biomedical.py, epistemic_contract.py, extract_contracts.py) lives inside respective domains/"
  artifacts:
    - path: "core/__init__.py"
      provides: "Core package marker"
    - path: "core/domain_resolver.py"
      provides: "Domain resolution infrastructure"
    - path: "domains/drug-discovery/domain.yaml"
      provides: "Drug discovery schema"
    - path: "domains/contracts/domain.yaml"
      provides: "Contract schema"
    - path: "examples/workbench/server.py"
      provides: "FastAPI workbench app"
  key_links:
    - from: "core/domain_resolver.py"
      to: "domains/"
      via: "DOMAINS_DIR constant"
      pattern: "DOMAINS_DIR.*Path.*domains"
    - from: "domains/contracts/epistemic.py"
      to: "scripts/epistemic_contract.py"
      via: "file move (was epistemic_contract.py)"
      pattern: "def analyze_contract_epistemic"
---

<objective>
Create the new directory structure (core/, domains/, examples/) and move all files to their target locations, updating internal references so files can be found at their new paths.

Purpose: Establishes the three-layer architecture (ARCH-01) and self-contained domain packages (ARCH-03) that all subsequent plans depend on.
Output: All files physically relocated with directory structure matching the target architecture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-repo-reorganization-and-cleanup/06-CONTEXT.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core/ package and move core scripts</name>
  <files>
    core/__init__.py
    core/domain_resolver.py
    core/build_extraction.py
    core/run_sift.py
    core/ingest_documents.py
    core/entity_resolution.py
    core/chunk_document.py
    core/label_communities.py
    core/label_epistemic.py
  </files>
  <read_first>
    scripts/domain_resolver.py
    scripts/build_extraction.py
    scripts/run_sift.py
    scripts/ingest_documents.py
    scripts/entity_resolution.py
    scripts/chunk_document.py
    scripts/label_communities.py
    scripts/label_epistemic.py
    scripts/__init__.py
  </read_first>
  <action>
    Per D-01, D-02, D-03: Create `core/` directory as a Python package.

    1. Create `core/__init__.py` with docstring: `"""Epistract core pipeline — domain-agnostic extraction, graph building, and epistemic dispatch."""`

    2. Use `git mv` to move these files from `scripts/` to `core/`:
       - `scripts/domain_resolver.py` -> `core/domain_resolver.py`
       - `scripts/build_extraction.py` -> `core/build_extraction.py`
       - `scripts/run_sift.py` -> `core/run_sift.py`
       - `scripts/ingest_documents.py` -> `core/ingest_documents.py`
       - `scripts/entity_resolution.py` -> `core/entity_resolution.py`
       - `scripts/chunk_document.py` -> `core/chunk_document.py`
       - `scripts/label_communities.py` -> `core/label_communities.py`
       - `scripts/label_epistemic.py` -> `core/label_epistemic.py`

    3. In `core/domain_resolver.py`, update the SKILLS_DIR constant (line 38):
       - FROM: `SKILLS_DIR = Path(__file__).parent.parent / "skills"`
       - TO: `DOMAINS_DIR = Path(__file__).parent.parent / "domains"`
       - Also rename all references from `SKILLS_DIR` to `DOMAINS_DIR` throughout the file.
       - Update the directory naming convention: the resolver currently looks for `skills/<name>-extraction/domain.yaml`. It needs to look for `domains/<name>/domain.yaml` instead. The domain directory names change:
         - `drug-discovery-extraction` -> `drug-discovery`
         - `contract-extraction` -> `contracts`
       - Update DEFAULT_DOMAIN to "drug-discovery" (already correct).
       - Update the `_domain_dir_name()` helper (or equivalent logic) to map domain names to directory names. Currently it appends "-extraction" to the name. Change to use the name directly. For "contract" -> "contracts" (plural), add a simple alias map: `DOMAIN_ALIASES = {"contract": "contracts"}`.

    4. In `core/run_sift.py`, update the import (line 20):
       - FROM: `from domain_resolver import resolve_domain, list_domains`
       - TO: `from core.domain_resolver import resolve_domain, list_domains`

    5. In `core/build_extraction.py`, update the import (line 14):
       - FROM: `from domain_resolver import resolve_domain`
       - TO: `from core.domain_resolver import resolve_domain`

    6. In `core/label_epistemic.py`, update imports (lines 30-31):
       - FROM: `from domain_resolver import resolve_domain` and `from epistemic_biomedical import analyze_biomedical_epistemic`
       - TO: `from core.domain_resolver import resolve_domain` and update the biomedical import to load dynamically from domains. Replace the static import with dynamic loading:
         ```python
         # Domain epistemic modules are loaded dynamically from domains/<name>/epistemic.py
         def _load_domain_epistemic(domain_name: str):
             """Load the epistemic analysis function for a domain."""
             domains_dir = Path(__file__).parent.parent / "domains"
             # Map domain names to directory names
             dir_map = {"drug-discovery": "drug-discovery", "contract": "contracts", "biomedical": "drug-discovery"}
             dir_name = dir_map.get(domain_name, domain_name)
             module_path = domains_dir / dir_name / "epistemic.py"
             if not module_path.exists():
                 return None
             import importlib.util
             spec = importlib.util.spec_from_file_location(f"domains.{dir_name}.epistemic", module_path)
             mod = importlib.util.module_from_spec(spec)
             spec.loader.exec_module(mod)
             return mod
         ```
         Then update the dispatch logic to use `_load_domain_epistemic()` instead of direct imports.

    7. Keep `scripts/setup.sh` in `scripts/` — it's a bash setup script, not core Python.
    8. Keep `scripts/validate_molecules.py` in `scripts/` temporarily — it will move to the drug-discovery domain in Task 2.
    9. Keep `scripts/launch_workbench.py` in `scripts/` temporarily — it moves in Task 3.
    10. Remove `scripts/__init__.py` after all moves are done (scripts/ is no longer a package).
  </action>
  <verify>
    <automated>test -d core && test -f core/__init__.py && test -f core/domain_resolver.py && test -f core/run_sift.py && test -f core/build_extraction.py && test -f core/label_epistemic.py && test -f core/label_communities.py && test -f core/ingest_documents.py && test -f core/entity_resolution.py && test -f core/chunk_document.py && python3 -c "import ast; ast.parse(open('core/domain_resolver.py').read()); print('syntax ok')" && grep -q "DOMAINS_DIR" core/domain_resolver.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `core/__init__.py` exists and contains docstring "domain-agnostic"
    - `core/domain_resolver.py` contains `DOMAINS_DIR = Path(__file__).parent.parent / "domains"` (not SKILLS_DIR)
    - `core/run_sift.py` contains `from core.domain_resolver import resolve_domain`
    - `core/build_extraction.py` contains `from core.domain_resolver import resolve_domain`
    - `core/label_epistemic.py` contains `_load_domain_epistemic` function
    - `core/label_epistemic.py` does NOT contain `from epistemic_biomedical import`
    - All 8 core files exist in `core/` and parse without syntax errors
    - `scripts/domain_resolver.py` no longer exists (moved via git mv)
  </acceptance_criteria>
  <done>core/ package exists with all 8 core scripts moved from scripts/, domain_resolver uses DOMAINS_DIR pointing to domains/, label_epistemic uses dynamic domain loading</done>
</task>

<task type="auto">
  <name>Task 2: Create domains/ structure and move domain packages</name>
  <files>
    domains/drug-discovery/domain.yaml
    domains/drug-discovery/SKILL.md
    domains/drug-discovery/references/entity-types.md
    domains/drug-discovery/references/relation-types.md
    domains/drug-discovery/validation/scan_patterns.py
    domains/drug-discovery/validation/validate_smiles.py
    domains/drug-discovery/validation/validate_sequences.py
    domains/drug-discovery/validation/__init__.py
    domains/drug-discovery/epistemic.py
    domains/contracts/domain.yaml
    domains/contracts/SKILL.md
    domains/contracts/references/entity-types.md
    domains/contracts/references/relation-types.md
    domains/contracts/epistemic.py
    domains/contracts/extract.py
    scripts/validate_molecules.py
    skills/drug-discovery-extraction
    skills/contract-extraction
  </files>
  <read_first>
    skills/drug-discovery-extraction/domain.yaml
    skills/drug-discovery-extraction/SKILL.md
    skills/contract-extraction/domain.yaml
    skills/contract-extraction/SKILL.md
    scripts/epistemic_biomedical.py
    scripts/epistemic_contract.py
    scripts/extract_contracts.py
    scripts/validate_molecules.py
    skills/drug-discovery-extraction/validation-scripts/scan_patterns.py
  </read_first>
  <action>
    Per D-04, D-05, D-06, D-12: Create self-contained domain packages under `domains/`.

    **Drug-discovery domain:**
    1. `mkdir -p domains/drug-discovery/references domains/drug-discovery/validation`
    2. `git mv skills/drug-discovery-extraction/domain.yaml domains/drug-discovery/domain.yaml`
    3. `git mv skills/drug-discovery-extraction/SKILL.md domains/drug-discovery/SKILL.md`
    4. `git mv skills/drug-discovery-extraction/references/entity-types.md domains/drug-discovery/references/entity-types.md`
    5. `git mv skills/drug-discovery-extraction/references/relation-types.md domains/drug-discovery/references/relation-types.md`
    6. `git mv skills/drug-discovery-extraction/validation-scripts/scan_patterns.py domains/drug-discovery/validation/scan_patterns.py`
    7. `git mv skills/drug-discovery-extraction/validation-scripts/validate_smiles.py domains/drug-discovery/validation/validate_smiles.py`
    8. `git mv skills/drug-discovery-extraction/validation-scripts/validate_sequences.py domains/drug-discovery/validation/validate_sequences.py`
    9. Create `domains/drug-discovery/validation/__init__.py` (empty, package marker)
    10. `git mv scripts/epistemic_biomedical.py domains/drug-discovery/epistemic.py`
    11. Move `scripts/validate_molecules.py` to `domains/drug-discovery/validate_molecules.py` via `git mv`. This orchestrator belongs with its validation scripts per D-05.

    **Contract domain:**
    1. `mkdir -p domains/contracts/references`
    2. `git mv skills/contract-extraction/domain.yaml domains/contracts/domain.yaml`
    3. `git mv skills/contract-extraction/SKILL.md domains/contracts/SKILL.md`
    4. `git mv skills/contract-extraction/references/entity-types.md domains/contracts/references/entity-types.md`
    5. `git mv skills/contract-extraction/references/relation-types.md domains/contracts/references/relation-types.md`
    6. `git mv scripts/epistemic_contract.py domains/contracts/epistemic.py`
    7. `git mv scripts/extract_contracts.py domains/contracts/extract.py`

    **Skills symlinks (per D-12):**
    Create symlinks from `skills/` to `domains/` so the Claude Code plugin runtime still discovers skills:
    1. Remove the now-empty `skills/drug-discovery-extraction/` and `skills/contract-extraction/` directories
    2. `ln -s ../domains/drug-discovery skills/drug-discovery-extraction`
    3. `ln -s ../domains/contracts skills/contract-extraction`

    **Update validate_molecules.py paths:**
    In `domains/drug-discovery/validate_molecules.py`, update the path that locates validation scripts:
    - FROM: looks for scripts in `skills/drug-discovery-extraction/validation-scripts/`
    - TO: looks for scripts in `domains/drug-discovery/validation/` (use `Path(__file__).parent / "validation"`)

    **Update domains/contracts/extract.py:** This file uses `sys.path` manipulation. Update any path references from `scripts/` to `core/` for core imports.

    **Cleanup:** Remove empty `skills/drug-discovery-extraction/validation-scripts/` directory and any `__pycache__` directories in moved locations.
  </action>
  <verify>
    <automated>test -f domains/drug-discovery/domain.yaml && test -f domains/drug-discovery/SKILL.md && test -f domains/drug-discovery/epistemic.py && test -f domains/drug-discovery/validation/scan_patterns.py && test -f domains/contracts/domain.yaml && test -f domains/contracts/SKILL.md && test -f domains/contracts/epistemic.py && test -f domains/contracts/extract.py && test -L skills/drug-discovery-extraction && test -L skills/contract-extraction && readlink skills/drug-discovery-extraction | grep -q "domains/drug-discovery" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `domains/drug-discovery/domain.yaml` exists (moved from skills/drug-discovery-extraction/)
    - `domains/drug-discovery/SKILL.md` exists
    - `domains/drug-discovery/validation/scan_patterns.py` exists
    - `domains/drug-discovery/validation/validate_smiles.py` exists
    - `domains/drug-discovery/validation/validate_sequences.py` exists
    - `domains/drug-discovery/epistemic.py` contains `def analyze_biomedical_epistemic`
    - `domains/drug-discovery/validate_molecules.py` exists and contains `Path(__file__).parent / "validation"`
    - `domains/contracts/domain.yaml` exists
    - `domains/contracts/epistemic.py` contains `def analyze_contract_epistemic`
    - `domains/contracts/extract.py` exists
    - `skills/drug-discovery-extraction` is a symlink pointing to `../domains/drug-discovery`
    - `skills/contract-extraction` is a symlink pointing to `../domains/contracts`
    - `skills/drug-discovery-extraction/domain.yaml` is readable via symlink
  </acceptance_criteria>
  <done>Both domain packages are self-contained under domains/, with skills/ symlinks preserving plugin discovery, and domain-specific Python (epistemic, extraction, validation) co-located with schemas</done>
</task>

<task type="auto">
  <name>Task 3: Move workbench to examples/ and update all command paths</name>
  <files>
    examples/workbench/__init__.py
    examples/workbench/server.py
    examples/workbench/api_chat.py
    examples/workbench/api_graph.py
    examples/workbench/api_sources.py
    examples/workbench/data_loader.py
    examples/workbench/system_prompt.py
    examples/workbench/static/
    scripts/launch_workbench.py
    commands/ingest.md
    commands/build.md
    commands/query.md
    commands/export.md
    commands/view.md
    commands/validate.md
    commands/setup.md
    commands/epistemic.md
    commands/dashboard.md
    commands/ask.md
  </files>
  <read_first>
    scripts/workbench/server.py
    scripts/workbench/api_chat.py
    scripts/workbench/system_prompt.py
    scripts/launch_workbench.py
    commands/ingest.md
    commands/build.md
    commands/query.md
    commands/export.md
    commands/view.md
    commands/validate.md
    commands/setup.md
    commands/epistemic.md
    commands/dashboard.md
    commands/ask.md
  </read_first>
  <action>
    Per D-11 (commands/ stays at root), ARCH-01 (examples/ for consumers):

    **Move workbench to examples/:**
    1. `mkdir -p examples/workbench`
    2. `git mv scripts/workbench/server.py examples/workbench/server.py`
    3. `git mv scripts/workbench/api_chat.py examples/workbench/api_chat.py`
    4. `git mv scripts/workbench/api_graph.py examples/workbench/api_graph.py`
    5. `git mv scripts/workbench/api_sources.py examples/workbench/api_sources.py`
    6. `git mv scripts/workbench/data_loader.py examples/workbench/data_loader.py`
    7. `git mv scripts/workbench/system_prompt.py examples/workbench/system_prompt.py`
    8. `git mv scripts/workbench/__init__.py examples/workbench/__init__.py`
    9. `git mv scripts/workbench/static examples/workbench/static`
    10. Remove empty `scripts/workbench/` directory.

    **Update workbench internal imports:**
    In `examples/workbench/server.py`, change:
    - `from scripts.workbench.api_chat import router as chat_router` -> `from examples.workbench.api_chat import router as chat_router`
    - `from scripts.workbench.api_graph import router as graph_router` -> `from examples.workbench.api_graph import router as graph_router`
    - `from scripts.workbench.api_sources import router as sources_router` -> `from examples.workbench.api_sources import router as sources_router`
    - `from scripts.workbench.data_loader import WorkbenchData` -> `from examples.workbench.data_loader import WorkbenchData`

    In `examples/workbench/api_chat.py`, change:
    - `from scripts.workbench.system_prompt import build_system_prompt, get_matched_source_chunks` -> `from examples.workbench.system_prompt import build_system_prompt, get_matched_source_chunks`

    In `examples/workbench/system_prompt.py`, change:
    - `from scripts.workbench.data_loader import WorkbenchData` -> `from examples.workbench.data_loader import WorkbenchData`

    **Update launch_workbench.py:**
    In `scripts/launch_workbench.py`, update the module path for uvicorn:
    - FROM: `"scripts.workbench.server:app"` (or however it references the server module)
    - TO: `"examples.workbench.server:app"`

    **Update ALL command paths:**
    In every `commands/*.md` file, replace `scripts/` paths with `core/` paths:
    - `${CLAUDE_PLUGIN_ROOT}/scripts/run_sift.py` -> `${CLAUDE_PLUGIN_ROOT}/core/run_sift.py`
    - `${CLAUDE_PLUGIN_ROOT}/scripts/build_extraction.py` -> `${CLAUDE_PLUGIN_ROOT}/core/build_extraction.py`
    - `${CLAUDE_PLUGIN_ROOT}/scripts/label_epistemic.py` -> `${CLAUDE_PLUGIN_ROOT}/core/label_epistemic.py`
    - `${CLAUDE_PLUGIN_ROOT}/scripts/validate_molecules.py` -> `${CLAUDE_PLUGIN_ROOT}/domains/drug-discovery/validate_molecules.py`
    - `${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh` -> `${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh` (unchanged)
    - `${CLAUDE_PLUGIN_ROOT}/scripts/launch_workbench.py` -> `${CLAUDE_PLUGIN_ROOT}/scripts/launch_workbench.py` (unchanged — launcher stays in scripts/)

    **Create examples/__init__.py:** Empty file, package marker.
  </action>
  <verify>
    <automated>test -f examples/workbench/server.py && test -f examples/workbench/api_chat.py && test -f examples/__init__.py && grep -q "examples.workbench" examples/workbench/server.py && grep -q "core/run_sift.py" commands/build.md && grep -q "core/run_sift.py" commands/query.md && grep -q "core/run_sift.py" commands/export.md && grep -q "core/run_sift.py" commands/view.md && grep -q "core/build_extraction.py" commands/ingest.md && grep -q "core/label_epistemic.py" commands/epistemic.md && ! test -d scripts/workbench && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/workbench/server.py` exists and contains `from examples.workbench.api_chat import`
    - `examples/workbench/api_chat.py` contains `from examples.workbench.system_prompt import`
    - `examples/workbench/system_prompt.py` contains `from examples.workbench.data_loader import`
    - `examples/__init__.py` exists
    - `scripts/workbench/` directory no longer exists
    - `commands/build.md` contains `core/run_sift.py` (not `scripts/run_sift.py`)
    - `commands/ingest.md` contains `core/build_extraction.py` and `core/run_sift.py`
    - `commands/query.md` contains `core/run_sift.py`
    - `commands/export.md` contains `core/run_sift.py`
    - `commands/view.md` contains `core/run_sift.py`
    - `commands/epistemic.md` contains `core/label_epistemic.py`
    - `commands/validate.md` contains `domains/drug-discovery/validate_molecules.py`
  </acceptance_criteria>
  <done>Workbench lives in examples/, all command markdown files reference core/ paths, all internal workbench imports use examples.workbench prefix</done>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
# Directory structure exists
test -d core && test -d domains/drug-discovery && test -d domains/contracts && test -d examples/workbench

# Core has no domain-specific imports
! grep -r "epistemic_biomedical\|epistemic_contract\|extract_contracts\|drug.discovery\|contract" core/*.py 2>/dev/null | grep -v "domain_resolver\|label_epistemic\|__pycache__\|#\|docstring\|comment\|DOMAIN_ALIASES\|dir_map"

# Skills symlinks work
test -f skills/drug-discovery-extraction/domain.yaml
test -f skills/contract-extraction/domain.yaml

# All Python files parse
python3 -c "
import ast, pathlib
for f in pathlib.Path('core').glob('*.py'):
    ast.parse(f.read_text())
    print(f'  {f}: OK')
for f in pathlib.Path('examples/workbench').glob('*.py'):
    ast.parse(f.read_text())
    print(f'  {f}: OK')
print('All files parse successfully')
"
```
</verification>

<success_criteria>
- Repository has core/, domains/, examples/ top-level directories
- 8 core scripts moved from scripts/ to core/
- 2 domain packages self-contained in domains/ with schemas, SKILL.md, references, and Python modules
- skills/ contains symlinks to domains/ for plugin discovery
- Workbench in examples/workbench/ with updated imports
- All command paths updated from scripts/ to core/
- All Python files parse without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-reorganization-and-cleanup/06-01-SUMMARY.md`
</output>
