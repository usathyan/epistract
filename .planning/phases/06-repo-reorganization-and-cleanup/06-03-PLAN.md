---
phase: 06-repo-reorganization-and-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "Stale V1 artifacts (paper/, medium/, poster/) are removed from the repo"
    - "Generated runtime artifacts (epistract-output/, docs/analysis/) are gitignored"
    - "DEVELOPER.md and docs/demo/ are removed"
    - "All V1 requirements in REQUIREMENTS.md are marked complete with phase references"
  artifacts:
    - path: ".gitignore"
      provides: "Updated ignore rules for removed directories"
      contains: "docs/analysis/"
    - path: ".planning/REQUIREMENTS.md"
      provides: "V1 requirements marked complete"
  key_links: []
---

<objective>
Remove stale V1 artifacts from the repository and mark V1 requirements as complete in REQUIREMENTS.md.

Purpose: Cleans the repo of publication artifacts, demo materials, and generated outputs that don't belong in a framework repository (CLEAN-01). Ensures traceability by marking all V1 requirements complete (CLEAN-02).
Output: Cleaner repository with only framework-relevant files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-repo-reorganization-and-cleanup/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale V1 artifacts</name>
  <files>
    .gitignore
  </files>
  <read_first>
    .gitignore
  </read_first>
  <action>
    Per D-07, D-08, D-09, D-10:

    **Remove publication artifacts (D-07):**
    ```bash
    git rm -r paper/
    git rm -r medium/
    git rm -r poster/
    ```

    **Remove generated runtime artifacts (D-08):**
    `epistract-output/` is already in .gitignore. Ensure it's removed from tracking if any files are tracked:
    ```bash
    git rm -r --cached epistract-output/ 2>/dev/null || true
    git rm -r --cached docs/analysis/ 2>/dev/null || true
    ```

    **Remove V1-specific docs (D-09):**
    ```bash
    git rm DEVELOPER.md
    git rm -r docs/demo/
    ```

    **Keep .planning/phases/01-05/ (D-10):** No action needed — already decided to keep.

    **Update .gitignore:** Add these entries if not already present:
    ```
    # V1 artifacts (removed in v2.0 Phase 6)
    paper/
    medium/
    poster/
    docs/analysis/
    docs/demo/
    DEVELOPER.md
    ```

    Verify `epistract-output/` and `docs/analysis/` are already in .gitignore. Current .gitignore has:
    - `/epistract-output/` — keep this
    - `docs/analysis/EricNotes.md` — replace with broader `docs/analysis/` if removing the whole directory

    Also add to .gitignore if not present:
    ```
    # Runtime outputs
    *.mp4
    ```
    (Already present in current .gitignore)

    **Remove other stale files:**
    - `docs/epistract-plugin-design.md` — V1 design doc, superseded by .planning/PROJECT.md
    - `docs/ADDING-DOMAINS.md` — V1 domain guide, will be replaced by Phase 10 docs
    - `docs/drug-discovery-domain-spec.md` — V1 domain spec, now in domains/drug-discovery/

    ```bash
    git rm docs/epistract-plugin-design.md 2>/dev/null || true
    git rm docs/ADDING-DOMAINS.md 2>/dev/null || true
    git rm docs/drug-discovery-domain-spec.md 2>/dev/null || true
    ```

    **Clean up docs/plans/ if empty or stale:**
    Check contents. If only V1 TODO files remain, remove them:
    ```bash
    ls docs/plans/ 2>/dev/null
    # Remove any completed TODO files
    ```

    **Do NOT remove:**
    - `.planning/` — GSD workflow history
    - `docs/diagrams/` — may be useful for Phase 10
    - `docs/superpowers/` — research prototypes, potentially useful
    - `tests/` — needed for Phase 7
    - `lib/` — vis.js needed by workbench
  </action>
  <verify>
    <automated>! test -d paper && ! test -d medium && ! test -d poster && ! test -f DEVELOPER.md && ! test -d docs/demo && grep -q "paper/" .gitignore && grep -q "medium/" .gitignore && grep -q "poster/" .gitignore && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `paper/` directory does not exist in the working tree
    - `medium/` directory does not exist in the working tree
    - `poster/` directory does not exist in the working tree
    - `DEVELOPER.md` does not exist in the working tree
    - `docs/demo/` directory does not exist in the working tree
    - `.gitignore` contains `paper/`, `medium/`, `poster/`
    - `.gitignore` contains `docs/analysis/`
    - `.planning/phases/` directories for phases 01-05 still exist (per D-10)
  </acceptance_criteria>
  <done>All stale V1 artifacts removed from repo, .gitignore updated to prevent re-addition</done>
</task>

<task type="auto">
  <name>Task 2: Mark V1 requirements complete in REQUIREMENTS.md</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Per CLEAN-02: Ensure all V1 requirements are marked complete with phase references.

    Read `.planning/REQUIREMENTS.md`. The V1 requirements section already shows all items checked (`[x]`). Verify:

    1. All 24 V1 requirements have `[x]` checkmarks:
       - DCFG-01 through DCFG-04 (Phase 1)
       - INGS-01 through INGS-10 (Phase 2)
       - EXTR-01, EXTR-02 (Phase 3)
       - GRPH-01, GRPH-02 (Phase 3)
       - XREF-01 through XREF-04 (Phase 4)
       - DASH-01, DASH-02 (Phase 5)

    2. The Traceability section has phase references for all V1 requirements. Currently shows:
       ```
       | DCFG-01..04 | Phase 1 | Complete |
       | INGS-01..10 | Phase 2 | Complete |
       ```
       This is correct but uses range notation. Expand if individual tracking is needed, or keep as-is if ranges are clear.

    3. Add a completion note at the top of the V1 section:
       ```
       ## v1 Requirements (Complete)

       All 24 requirements delivered across Phases 1-5. Shipped 2026-04-02.
       See v1 traceability below.
       ```
       (This is already approximately what's there — verify and make minor wording adjustments if needed.)

    4. In the v2 Traceability table, update CLEAN-02 status:
       - FROM: `| CLEAN-02 | Phase 6 | — | Pending |`
       - TO: `| CLEAN-02 | Phase 6 | 06-03 | In Progress |`

    If the file already looks correct, make minimal or no changes. The key is verification that the state is accurate.
  </action>
  <verify>
    <automated>grep -c "\[x\]" .planning/REQUIREMENTS.md | xargs -I{} test {} -ge 24 && grep -q "All 24 requirements delivered" .planning/REQUIREMENTS.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains at least 24 lines with `[x]` (all V1 requirements checked)
    - `.planning/REQUIREMENTS.md` contains text "All 24 requirements delivered"
    - V1 traceability table shows "Complete" for all V1 requirement ranges
  </acceptance_criteria>
  <done>All V1 requirements verified as marked complete with phase references, REQUIREMENTS.md is accurate</done>
</task>

</tasks>

<verification>
```bash
# Stale artifacts gone
! test -d paper && ! test -d medium && ! test -d poster && ! test -f DEVELOPER.md

# .gitignore updated
grep -q "paper/" .gitignore
grep -q "medium/" .gitignore

# V1 requirements marked complete
grep -c "\[x\]" .planning/REQUIREMENTS.md  # Should be >= 24

# Planning history preserved (D-10)
test -d .planning/phases/01-domain-configuration
```
</verification>

<success_criteria>
- paper/, medium/, poster/, DEVELOPER.md, docs/demo/ removed from repo
- .gitignore prevents re-addition of removed directories
- All 24 V1 requirements marked [x] in REQUIREMENTS.md
- .planning/ history preserved intact
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-reorganization-and-cleanup/06-03-SUMMARY.md`
</output>
