---
phase: 07-testing-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - pyproject.toml
  - tests/test_unit.py
  - tests/fixtures/contract_graph_data.json
  - tests/fixtures/contract_claims_layer.json
  - tests/fixtures/sample_extraction_drug.json
  - tests/fixtures/sample_extraction_contract.json
  - tests/test_schemas.py
autonomous: true
requirements:
  - TEST-01
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "All 14 existing unit tests pass with @pytest.mark.unit marker against reorganized codebase"
    - "Domain YAML schemas for both drug-discovery and contracts load without error"
    - "DocumentExtraction Pydantic model validates existing extraction JSON fixtures"
    - "pytest markers (unit, integration, e2e) are registered without warnings"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared fixtures, path setup, availability flags"
      contains: "FIXTURES_DIR"
    - path: "pyproject.toml"
      provides: "Pytest marker registration and config"
      contains: "tool.pytest.ini_options"
    - path: "tests/test_schemas.py"
      provides: "Pydantic schema validation and domain YAML tests"
      contains: "class DocumentExtraction"
    - path: "tests/fixtures/contract_graph_data.json"
      provides: "Contract domain graph fixture for cross-domain tests"
      contains: "PARTY"
    - path: "tests/fixtures/sample_extraction_drug.json"
      provides: "Pre-recorded drug extraction for E2E pipeline"
      contains: "drug-discovery"
    - path: "tests/fixtures/sample_extraction_contract.json"
      provides: "Pre-recorded contract extraction for E2E pipeline"
      contains: "contracts"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/fixtures/"
      via: "FIXTURES_DIR constant"
      pattern: "FIXTURES_DIR.*fixtures"
    - from: "tests/test_unit.py"
      to: "tests/conftest.py"
      via: "pytest auto-import"
      pattern: "pytest.mark.unit"
---

<objective>
Test infrastructure, fixtures, and unit tier with markers. Creates conftest.py with shared fixtures and path setup, pyproject.toml for pytest config, contract domain fixtures, pre-recorded extraction JSONs for E2E, and a schema validation test file for Pydantic DocumentExtraction + domain YAML tests. Adds @pytest.mark.unit to all 14 existing tests.

Purpose: Foundation that all other test plans depend on. Without conftest.py and fixtures, no integration or E2E tests can run.
Output: conftest.py, pyproject.toml, test_schemas.py, 4 new fixture files, markers on existing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-testing-framework/07-CONTEXT.md
@.planning/phases/07-testing-framework/07-RESEARCH.md

@tests/test_unit.py
@tests/fixtures/sample_graph_data.json
@core/build_extraction.py
@core/domain_resolver.py
@domains/contracts/domain.yaml
@domains/drug-discovery/domain.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conftest.py, pyproject.toml, and fixture files</name>
  <files>tests/conftest.py, pyproject.toml, tests/fixtures/contract_graph_data.json, tests/fixtures/contract_claims_layer.json, tests/fixtures/sample_extraction_drug.json, tests/fixtures/sample_extraction_contract.json</files>
  <read_first>
    - tests/test_unit.py (lines 1-60 for existing path setup and availability flags to centralize)
    - tests/fixtures/sample_graph_data.json (existing graph fixture format to replicate for contract domain)
    - tests/fixtures/sample_claims_layer.json (existing claims fixture format to replicate)
    - core/build_extraction.py (write_extraction function signature and output JSON format)
    - core/domain_resolver.py (DOMAINS_DIR resolution path to understand import needs)
    - domains/contracts/domain.yaml (9 entity types: PARTY, CONTRACT, OBLIGATION, DEADLINE, COST, VENUE, SERVICE, INSURANCE, PENALTY and 9 relation types to use in contract fixtures)
    - domains/drug-discovery/domain.yaml (entity types for drug extraction fixture)
  </read_first>
  <action>
    **1. Create `tests/conftest.py`** with:
    - `PROJECT_ROOT = Path(__file__).parent.parent`
    - `FIXTURES_DIR = Path(__file__).parent / "fixtures"`
    - `sys.path.insert(0, str(PROJECT_ROOT / "core"))` — centralize ALL path setup here
    - `sys.path.insert(0, str(PROJECT_ROOT / "domains" / "drug-discovery" / "validation"))`
    - `sys.path.insert(0, str(PROJECT_ROOT / "domains" / "drug-discovery"))`
    - `sys.path.insert(0, str(PROJECT_ROOT / "domains" / "contracts"))`
    - Availability flags: `HAS_SIFTKG`, `HAS_RDKIT`, `HAS_BIOPYTHON` with try/except pattern (per D-10)
    - Fixtures: `fixtures_dir()`, `sample_graph_data()`, `sample_claims_layer()`, `sample_communities()`, `contract_graph_data()`, `contract_claims_layer()`, `sample_output_dir(tmp_path)` (copies fixture files to tmp_path, creates `ingested/` subdir)
    - Export availability flags as module-level constants so test files can import them

    **2. Create `pyproject.toml`** with minimal pytest config (per D-01):
    ```toml
    [tool.pytest.ini_options]
    markers = [
        "unit: Fast tests with no external dependencies",
        "integration: Tests requiring sift-kg and optional libs",
        "e2e: End-to-end pipeline tests",
    ]
    testpaths = ["tests"]
    ```

    **3. Create `tests/fixtures/contract_graph_data.json`** — contract domain graph fixture with:
    - 5 nodes using contract entity types: 2 PARTY nodes (e.g., "Pennsylvania Convention Center Authority", "Aramark"), 1 OBLIGATION node, 1 DEADLINE node, 1 COST node
    - Each node has: id, name, entity_type, confidence (0.9+), source_documents (list of filenames), community (int), attributes (dict)
    - 4 edges: OBLIGATED_TO, HAS_DEADLINE, COSTS, PROVIDES_SERVICE
    - Each edge has: source, target, relation_type, confidence, evidence (string)
    - Top-level `metadata` key with `domain_name: "contracts"`, `document_count: 2`, `entity_count: 5`, `relation_count: 4`

    **4. Create `tests/fixtures/contract_claims_layer.json`** — contract domain claims fixture with:
    - `claims` list with 3 entries, each having: `claim_id`, `source_entity`, `target_entity`, `relation_type`, `evidence`, `confidence`, `epistemic_status` (one each of "asserted", "conditional", "obligatory")
    - `summary` dict with `total_claims: 3`, `by_status` counts

    **5. Create `tests/fixtures/sample_extraction_drug.json`** — pre-recorded drug-discovery DocumentExtraction:
    - `document_id: "test_drug_paper.pdf"`
    - `domain_name: "drug-discovery"`
    - `entities`: 4 entities — 1 COMPOUND (name: "sotorasib"), 1 GENE (name: "KRAS"), 1 DISEASE (name: "non-small cell lung cancer"), 1 MECHANISM (name: "covalent inhibition")
    - Each entity: `name`, `entity_type`, `confidence` (0.85-0.97), `context` (sentence fragment)
    - `relations`: 3 relations — TARGETS (sotorasib -> KRAS), TREATS (sotorasib -> NSCLC), HAS_MECHANISM (sotorasib -> covalent inhibition)
    - Each relation: `source_entity`, `target_entity`, `relation_type`, `confidence`, `evidence`
    - `extracted_at`: ISO timestamp, `chunks_processed: 1`

    **6. Create `tests/fixtures/sample_extraction_contract.json`** — pre-recorded contract DocumentExtraction:
    - `document_id: "test_vendor_agreement.pdf"`
    - `domain_name: "contracts"`
    - `entities`: 4 entities — 2 PARTY, 1 OBLIGATION, 1 COST
    - `relations`: 3 relations — OBLIGATED_TO, COSTS, PROVIDES_SERVICE
    - Same field structure as drug extraction
    - `extracted_at`: ISO timestamp, `chunks_processed: 1`
  </action>
  <verify>
    <automated>cd /Users/umeshbhatt/code/epistract && python -c "import json; from pathlib import Path; f=Path('tests/fixtures'); assert (f/'contract_graph_data.json').exists(); assert (f/'sample_extraction_drug.json').exists(); d=json.loads((f/'contract_graph_data.json').read_text()); assert len(d['nodes'])>=5; assert 'metadata' in d; print('All fixtures valid')" && python -c "import sys; sys.path.insert(0,'tests'); import conftest; print('conftest imports ok')" && python -m pytest --co -q tests/test_unit.py 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `FIXTURES_DIR = Path(__file__).parent / "fixtures"`
    - tests/conftest.py contains `HAS_SIFTKG`, `HAS_RDKIT`, `HAS_BIOPYTHON` flags
    - tests/conftest.py contains `def sample_graph_data()` and `def contract_graph_data()` fixtures
    - tests/conftest.py contains `sys.path.insert(0, str(PROJECT_ROOT / "core"))`
    - pyproject.toml contains `[tool.pytest.ini_options]`
    - pyproject.toml contains `"unit: Fast tests with no external dependencies"`
    - tests/fixtures/contract_graph_data.json contains `"entity_type": "PARTY"` and `"metadata"`
    - tests/fixtures/contract_claims_layer.json contains `"epistemic_status"` and `"claims"`
    - tests/fixtures/sample_extraction_drug.json contains `"domain_name": "drug-discovery"` and `"entity_type": "COMPOUND"`
    - tests/fixtures/sample_extraction_contract.json contains `"domain_name": "contracts"` and `"entity_type": "PARTY"`
  </acceptance_criteria>
  <done>conftest.py centralizes all path setup and fixtures; pyproject.toml registers 3 markers; 4 new fixture files exist with correct structure</done>
</task>

<task type="auto">
  <name>Task 2: Add unit markers to existing tests and create schema validation tests</name>
  <files>tests/test_unit.py, tests/test_schemas.py</files>
  <read_first>
    - tests/test_unit.py (full file — need to see every test function to add markers and remove duplicated path setup)
    - tests/conftest.py (just created — verify available fixtures and path setup)
    - core/build_extraction.py (write_extraction signature and JSON output format for Pydantic model)
    - domains/drug-discovery/domain.yaml (entity_types list to validate in skill test)
    - domains/contracts/domain.yaml (entity_types list to validate in skill test)
    - domains/drug-discovery/SKILL.md (verify it exists for skill coverage)
    - domains/contracts/SKILL.md (verify it exists for skill coverage)
  </read_first>
  <action>
    **1. Update `tests/test_unit.py`** (per D-01, D-10):
    - Remove the sys.path setup block (lines 16-25) — now handled by conftest.py
    - Remove the availability flag block (lines 30-46) — now in conftest.py. Instead, import from conftest: `from conftest import HAS_SIFTKG, HAS_RDKIT, HAS_BIOPYTHON`
    - Add `@pytest.mark.unit` decorator to every test function (all 14 tests: test_ut001 through test_ut014 or however they're named)
    - Keep all existing test logic unchanged — only add markers and remove duplicate setup
    - Keep the conditional imports for validate_smiles, validate_sequence (lines 56-59) — those depend on availability flags

    **2. Create `tests/test_schemas.py`** — schema validation tests for TEST-03 (skill coverage) and TEST-04 (agent coverage):

    ```python
    # Pydantic models for DocumentExtraction (per D-07):
    class ExtractedEntity(BaseModel):
        name: str
        entity_type: str
        confidence: float
        context: str = ""

    class ExtractedRelation(BaseModel):
        source_entity: str
        target_entity: str
        relation_type: str
        confidence: float
        evidence: str = ""

    class DocumentExtraction(BaseModel):
        document_id: str
        entities: list[ExtractedEntity]
        relations: list[ExtractedRelation]
        extracted_at: str
        domain_name: str
        chunks_processed: int = 1
        document_path: str = ""
        cost_usd: float = 0.0
        model_used: str = ""
        chunk_size: int = 10000
        error: str | None = None
    ```

    Unit-tier tests (all @pytest.mark.unit):

    **TEST-04 tests (agent coverage via Pydantic):**
    - `test_extraction_schema_drug_discovery`: Load `sample_extraction_drug.json` fixture, validate with `DocumentExtraction(**data)`. Assert no ValidationError. Assert `len(result.entities) >= 3`. Assert `result.domain_name == "drug-discovery"`.
    - `test_extraction_schema_contracts`: Load `sample_extraction_contract.json` fixture, validate with `DocumentExtraction(**data)`. Assert no ValidationError. Assert `result.domain_name == "contracts"`.
    - `test_extraction_schema_rejects_invalid`: Create dict with missing required `document_id` field, assert `ValidationError` is raised.
    - `test_extraction_entity_types_valid`: Load drug extraction fixture, validate every entity's `entity_type` is in the set `{"COMPOUND", "GENE", "DISEASE", "MECHANISM", "PATHWAY", "CLINICAL_TRIAL", "SIDE_EFFECT", "DOSAGE", "BIOMARKER", "PROTEIN", "CELL_LINE", "ORGANISM", "CHEMICAL_STRUCTURE", "NUCLEOTIDE_SEQUENCE", "DRUG_CLASS", "PUBLICATION", "REGULATORY_STATUS"}`.

    **TEST-03 tests (skill coverage via YAML):**
    - `test_drug_discovery_domain_yaml_loads`: `yaml.safe_load()` on `domains/drug-discovery/domain.yaml`. Assert `data["name"]` exists. Assert `len(data["entity_types"]) == 17`. Assert `len(data["relation_types"]) == 30`.
    - `test_contracts_domain_yaml_loads`: `yaml.safe_load()` on `domains/contracts/domain.yaml`. Assert `data["name"]` exists. Assert `len(data["entity_types"]) == 9`. Assert `len(data["relation_types"]) == 9`.
    - `test_drug_discovery_skill_md_exists`: Assert `(PROJECT_ROOT / "domains" / "drug-discovery" / "SKILL.md").exists()`. Assert file size > 100 bytes.
    - `test_contracts_skill_md_exists`: Assert `(PROJECT_ROOT / "domains" / "contracts" / "SKILL.md").exists()`. Assert file size > 100 bytes.
    - `test_domain_yaml_entity_types_have_required_fields`: For each domain YAML, every entity_type entry must have `name` and `description` keys.
    - `test_domain_yaml_relation_types_have_required_fields`: For each domain YAML, every relation_type entry must have `name` and `description` keys.
  </action>
  <verify>
    <automated>cd /Users/umeshbhatt/code/epistract && python -m pytest tests/test_unit.py -m unit -v --tb=short 2>&1 | tail -20 && python -m pytest tests/test_schemas.py -m unit -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_unit.py does NOT contain `sys.path.insert` (path setup moved to conftest)
    - tests/test_unit.py contains `@pytest.mark.unit` on every test function
    - tests/test_schemas.py contains `class DocumentExtraction(BaseModel)`
    - tests/test_schemas.py contains `def test_extraction_schema_drug_discovery`
    - tests/test_schemas.py contains `def test_extraction_schema_contracts`
    - tests/test_schemas.py contains `def test_drug_discovery_domain_yaml_loads`
    - tests/test_schemas.py contains `def test_contracts_domain_yaml_loads`
    - `python -m pytest tests/test_unit.py -m unit -v` exits 0 (all 14 existing tests pass with markers)
    - `python -m pytest tests/test_schemas.py -m unit -v` exits 0 (all schema/skill tests pass)
  </acceptance_criteria>
  <done>All 14 existing unit tests decorated with @pytest.mark.unit and passing. Schema validation tests verify DocumentExtraction Pydantic model against both domain fixtures. Domain YAML and SKILL.md tests verify all skills load correctly.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_unit.py -m unit -v` — all 14 existing tests pass (TEST-01 partial)
- `python -m pytest tests/test_schemas.py -m unit -v` — schema and skill tests pass (TEST-03, TEST-04)
- `python -m pytest tests/ -m unit --co -q` — all unit tests collected without marker warnings
- All 4 new fixture files parse as valid JSON
</verification>

<success_criteria>
- conftest.py centralizes path setup, availability flags, and fixture loading for all test tiers
- pyproject.toml registers unit/integration/e2e markers
- All 14 existing unit tests pass with @pytest.mark.unit marker
- DocumentExtraction Pydantic model validates both drug-discovery and contract extraction fixtures
- Both domain YAMLs load and have correct entity/relation type counts
- Both domain SKILL.md files exist and are non-trivial
- 4 new fixture files created with correct structure
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-framework/07-01-SUMMARY.md`
</output>
