---
phase: 07-testing-framework
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/test_integration.py
  - tests/test_kg_provenance.py
autonomous: true
requirements:
  - TEST-02
  - TEST-06
  - TEST-07

must_haves:
  truths:
    - "Every testable /epistract:* command entry point runs without error against fixtures"
    - "All 32 KG provenance tests pass offline using fixture data instead of live server"
    - "Both drug-discovery and contracts domains produce valid graphs through the same pipeline entry point"
  artifacts:
    - path: "tests/test_integration.py"
      provides: "Integration tier tests for command entry points and cross-domain verification"
      contains: "pytest.mark.integration"
    - path: "tests/test_kg_provenance.py"
      provides: "32 provenance tests converted to fixture-based mocking"
      contains: "FIXTURES_DIR"
  key_links:
    - from: "tests/test_integration.py"
      to: "core/run_sift.py"
      via: "imports cmd_build, cmd_search, cmd_export"
      pattern: "from run_sift import"
    - from: "tests/test_integration.py"
      to: "core/domain_resolver.py"
      via: "resolve_domain for cross-domain test"
      pattern: "resolve_domain"
    - from: "tests/test_kg_provenance.py"
      to: "tests/fixtures/sample_graph_data.json"
      via: "fixture loading replaces HTTP calls"
      pattern: "FIXTURES_DIR"
---

<objective>
Integration tier tests and KG provenance conversion. Creates test_integration.py with command entry point tests (TEST-02) and cross-domain verification (TEST-07). Converts 32 provenance tests from live-server HTTP to fixture-based mocking (TEST-06).

Purpose: Validates that all core pipeline functions work correctly against fixtures and that both domains produce valid output through the same pipeline.
Output: test_integration.py (new), test_kg_provenance.py (converted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-testing-framework/07-CONTEXT.md
@.planning/phases/07-testing-framework/07-RESEARCH.md

@tests/conftest.py
@tests/test_kg_provenance.py
@core/run_sift.py
@core/build_extraction.py
@core/label_epistemic.py
@core/label_communities.py
@core/domain_resolver.py
@core/ingest_documents.py
@domains/drug-discovery/validate_molecules.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test file for command coverage and cross-domain verification</name>
  <files>tests/test_integration.py</files>
  <read_first>
    - tests/conftest.py (available fixtures: sample_output_dir, fixtures_dir, HAS_SIFTKG, sample_graph_data, contract_graph_data)
    - core/run_sift.py (full file — find cmd_build, cmd_search, cmd_export, cmd_info function signatures and required arguments)
    - core/build_extraction.py (write_extraction function signature — args: entities, relations, doc_id, output_dir, domain_name)
    - core/label_epistemic.py (analyze_epistemic or main entry point signature)
    - core/label_communities.py (label_communities or main entry point signature)
    - core/domain_resolver.py (resolve_domain signature and return type — returns dict with yaml_path key)
    - core/ingest_documents.py (main entry point for /epistract:ingest)
    - domains/drug-discovery/validate_molecules.py (main entry point for /epistract:validate)
    - tests/fixtures/sample_extraction_drug.json (fixture structure for build test)
    - tests/fixtures/sample_extraction_contract.json (fixture structure for cross-domain test)
  </read_first>
  <action>
    Create `tests/test_integration.py` with `@pytest.mark.integration` on all tests. Use `pytest.mark.skipif(not HAS_SIFTKG, reason="sift-kg not installed")` where sift-kg imports are needed (per D-10).

    Import from conftest: `from conftest import HAS_SIFTKG, HAS_RDKIT, FIXTURES_DIR, PROJECT_ROOT`

    **TEST-02: Command entry point tests (per D-06):**

    1. `test_build_extraction_writes_json(tmp_path)`:
       - Import `write_extraction` from `build_extraction`
       - Call `write_extraction(entities=[{"name": "TestEntity", "entity_type": "PARTY", "confidence": 0.9, "context": "test"}], relations=[], doc_id="test_doc.pdf", output_dir=str(tmp_path), domain_name="contracts")`
       - Assert `(tmp_path / "extractions" / "test_doc.pdf.json").exists()`
       - Load the JSON, assert `data["document_id"] == "test_doc.pdf"` and `len(data["entities"]) == 1`

    2. `test_cmd_build_from_fixture_extractions(tmp_path)`:
       - skipif not HAS_SIFTKG
       - Copy `sample_extraction_drug.json` to `tmp_path / "extractions" / "test_doc.json"`
       - Import `cmd_build` from `run_sift` and call it with `output_dir=str(tmp_path)`, `domain_name="drug-discovery"`
       - Assert `(tmp_path / "graph_data.json").exists()`
       - Load graph JSON, assert `len(data["nodes"]) > 0`

    3. `test_cmd_search_finds_entity(tmp_path)`:
       - skipif not HAS_SIFTKG
       - Copy `sample_graph_data.json` fixture to `tmp_path / "graph_data.json"`
       - Import `cmd_search` from `run_sift`, call with a known entity name from the fixture (e.g., "Pennsylvania Convention Center Authority") and `output_dir=str(tmp_path)`
       - Assert result is not empty (function returns list or prints results — check actual signature)

    4. `test_cmd_export_creates_output(tmp_path)`:
       - skipif not HAS_SIFTKG
       - Copy `sample_graph_data.json` fixture to `tmp_path / "graph_data.json"`
       - Import `cmd_export` from `run_sift`, call with `output_dir=str(tmp_path)`, `format="json"`
       - Assert an export file is created in tmp_path

    5. `test_label_epistemic_produces_claims(tmp_path)`:
       - Copy `sample_graph_data.json` to `tmp_path / "graph_data.json"`
       - Import entry point from `label_epistemic` and call with `tmp_path`
       - Assert `(tmp_path / "claims_layer.json").exists()`

    6. `test_label_communities_produces_output(tmp_path)`:
       - skipif not HAS_SIFTKG
       - Copy `sample_graph_data.json` to `tmp_path / "graph_data.json"`
       - Import entry point from `label_communities` and call with `tmp_path`
       - Assert `(tmp_path / "communities.json").exists()` OR verify the function completes without error

    7. `test_domain_resolver_resolves_both_domains()`:
       - Import `resolve_domain` from `domain_resolver`
       - `result = resolve_domain("drug-discovery")` — assert `result` is dict, assert `"yaml_path" in result`
       - `result = resolve_domain("contracts")` — assert `result` is dict, assert `"yaml_path" in result`
       - Assert both yaml_path values point to existing files

    8. `test_validate_molecules_runs_without_error(tmp_path)`:
       - skipif not HAS_RDKIT
       - Copy drug extraction fixture to `tmp_path / "extractions" / "test.json"`
       - Import main validation function from `validate_molecules`
       - Call with tmp_path, assert no exception raised

    **TEST-07: Cross-domain verification (per D-05):**

    9. `test_cross_domain_both_produce_valid_graphs(tmp_path)`:
       - skipif not HAS_SIFTKG
       - For each domain in `["drug-discovery", "contracts"]`:
         - Create `domain_dir = tmp_path / domain_name_safe` and `domain_dir / "extractions"`
         - Copy the domain's extraction fixture (sample_extraction_drug.json or sample_extraction_contract.json) to `domain_dir / "extractions" / "test.json"`
         - Call `cmd_build(str(domain_dir), domain_name=domain_name)`
         - Load `domain_dir / "graph_data.json"`, assert `len(data["nodes"]) > 0`
         - Assert `"metadata" in data` or graph has expected structure

    10. `test_cross_domain_resolver_loads_correct_schemas()`:
        - For each domain in `["drug-discovery", "contracts"]`:
          - `result = resolve_domain(domain_name)`
          - Load the YAML at `result["yaml_path"]`
          - Assert `data["entity_types"]` is a non-empty list
          - Assert `data["relation_types"]` is a non-empty list

    NOTE: Read the actual function signatures from the source files before writing tests. The test calls must match the real API. If `cmd_build` takes different args than described, adapt accordingly. If functions print to stdout rather than return values, verify side effects (file creation) instead of return values.
  </action>
  <verify>
    <automated>cd /Users/umeshbhatt/code/epistract && python -m pytest tests/test_integration.py -m integration -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_integration.py contains `@pytest.mark.integration` on every test function
    - tests/test_integration.py contains `def test_build_extraction_writes_json`
    - tests/test_integration.py contains `def test_cmd_build_from_fixture_extractions`
    - tests/test_integration.py contains `def test_cross_domain_both_produce_valid_graphs`
    - tests/test_integration.py contains `def test_cross_domain_resolver_loads_correct_schemas`
    - tests/test_integration.py contains `def test_domain_resolver_resolves_both_domains`
    - tests/test_integration.py contains `skipif(not HAS_SIFTKG` for sift-kg dependent tests
    - `python -m pytest tests/test_integration.py -m integration -v` exits 0 (or only skipped tests for missing optional deps)
  </acceptance_criteria>
  <done>Integration test file covers all 7 testable command entry points plus 2 cross-domain verification tests. Tests pass (or skip cleanly for missing optional deps).</done>
</task>

<task type="auto">
  <name>Task 2: Convert KG provenance tests from live server to fixture-based</name>
  <files>tests/test_kg_provenance.py</files>
  <read_first>
    - tests/test_kg_provenance.py (full file — understand all 32 tests, HTTP call patterns, graph_data/graph_index fixtures, chat fixture, entity lookup logic)
    - tests/conftest.py (available fixtures: sample_graph_data, sample_claims_layer, FIXTURES_DIR)
    - tests/fixtures/sample_graph_data.json (the graph structure tests will use)
    - tests/fixtures/sample_claims_layer.json (the claims structure tests will use)
  </read_first>
  <action>
    Convert `tests/test_kg_provenance.py` from live HTTP to fixture-based (per D-03):

    **Remove:**
    - `import requests` line
    - `BASE_URL` and `OUTPUT_DIR` constants
    - The `graph_data()` fixture that calls `requests.get(f"{BASE_URL}/api/graph")`
    - Any other `requests.get` or `requests.post` calls (chat API calls)

    **Replace graph_data fixture:**
    ```python
    @pytest.fixture(scope="module")
    def graph_data():
        """Load graph data from test fixtures (per D-03: no live server)."""
        return json.loads((FIXTURES_DIR / "sample_graph_data.json").read_text())
    ```
    Import `FIXTURES_DIR` from conftest.

    **Replace chat fixtures:**
    The 32 tests ask questions via `/api/chat` and verify entities in responses trace to graph nodes. Since we're removing HTTP calls, create pre-recorded chat response fixtures. For each test that currently calls `/api/chat`:
    - Replace the HTTP call with a hardcoded response string that mentions entities from `sample_graph_data.json`
    - OR create a `mock_chat_responses` dict mapping question patterns to response strings
    - The test should still verify: (a) entities mentioned in the response exist as graph nodes, (b) relationships claimed exist as edges, (c) source documents are traceable

    **Keep intact:**
    - `graph_index` fixture (builds node/edge lookup from graph_data — this is the core provenance logic)
    - All `find_node()`, `find_edges_between()`, entity matching helper functions
    - The assertion logic in each test (node existence, edge existence, source document checks)
    - The test names and IDs

    **Add markers:**
    - Add `@pytest.mark.integration` to all 32 tests (per D-01 — these need graph data fixtures loaded)
    - Actually reconsider: if tests only load JSON and do dict lookups, they could be `@pytest.mark.unit`. Use unit if no sift-kg imports are needed.

    **Key insight from research:** The converted tests verify graph tracing logic (node lookup, edge traversal, entity matching), NOT the API layer. This is an accepted scope change per RESEARCH.md pitfall 2. The tests prove that given a graph and a chat response, provenance tracing works correctly.
  </action>
  <verify>
    <automated>cd /Users/umeshbhatt/code/epistract && python -m pytest tests/test_kg_provenance.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_kg_provenance.py does NOT contain `import requests`
    - tests/test_kg_provenance.py does NOT contain `BASE_URL`
    - tests/test_kg_provenance.py contains `FIXTURES_DIR` (loads from fixtures)
    - tests/test_kg_provenance.py contains `pytest.mark.unit` or `pytest.mark.integration` on every test
    - tests/test_kg_provenance.py still contains `graph_index` fixture with node/edge lookup logic
    - `python -m pytest tests/test_kg_provenance.py -v` exits 0 (all 32 tests pass offline)
    - tests/test_kg_provenance.py does NOT call `requests.get` or `requests.post` anywhere
  </acceptance_criteria>
  <done>All 32 KG provenance tests pass offline using fixture data. No HTTP calls. Graph tracing logic (node lookup, edge traversal, source document verification) preserved.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_integration.py -m integration -v` — command and cross-domain tests pass (TEST-02, TEST-07)
- `python -m pytest tests/test_kg_provenance.py -v` — all 32 provenance tests pass offline (TEST-06)
- No test requires a running server or network access
</verification>

<success_criteria>
- 7 testable command entry points have integration tests that run without error
- 2 cross-domain tests verify both domains produce valid graphs through same pipeline
- All 32 KG provenance tests pass offline with fixture-based data
- No HTTP calls or live server dependencies remain in any test
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-framework/07-02-SUMMARY.md`
</output>
