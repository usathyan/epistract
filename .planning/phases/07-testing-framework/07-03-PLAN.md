---
phase: 07-testing-framework
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/test_e2e.py
  - Makefile
autonomous: true
requirements:
  - TEST-05
  - TEST-01

must_haves:
  truths:
    - "E2E pipeline test proves full lifecycle: extraction JSON -> graph build -> epistemic -> export"
    - "make test runs unit tier only and exits 0"
    - "make test-integration runs integration tier"
    - "make test-all runs all tiers"
  artifacts:
    - path: "tests/test_e2e.py"
      provides: "End-to-end pipeline test for full lifecycle"
      contains: "pytest.mark.e2e"
    - path: "Makefile"
      provides: "Tiered test targets"
      contains: "test-integration"
  key_links:
    - from: "tests/test_e2e.py"
      to: "core/run_sift.py"
      via: "cmd_build and cmd_export calls"
      pattern: "from run_sift import"
    - from: "tests/test_e2e.py"
      to: "core/label_epistemic.py"
      via: "epistemic analysis step"
      pattern: "label_epistemic"
    - from: "Makefile"
      to: "tests/"
      via: "pytest -m marker selection"
      pattern: "pytest.*-m"
---

<objective>
E2E pipeline test and Makefile targets. Creates test_e2e.py with a full lifecycle test from pre-recorded extraction through graph build, epistemic analysis, and export (TEST-05). Updates Makefile with tiered test targets (per D-02). Also serves as final TEST-01 validation since make test must run all unit tests.

Purpose: Proves the complete pipeline works end-to-end and provides developer-friendly make targets for running different test tiers.
Output: test_e2e.py (new), Makefile (updated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-testing-framework/07-CONTEXT.md
@.planning/phases/07-testing-framework/07-RESEARCH.md

@tests/conftest.py
@Makefile
@core/run_sift.py
@core/build_extraction.py
@core/label_epistemic.py
@core/label_communities.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E pipeline test</name>
  <files>tests/test_e2e.py</files>
  <read_first>
    - tests/conftest.py (available fixtures and HAS_SIFTKG flag)
    - core/run_sift.py (cmd_build and cmd_export function signatures and args)
    - core/build_extraction.py (write_extraction signature)
    - core/label_epistemic.py (entry point function signature and what it expects in the directory)
    - core/label_communities.py (entry point function signature)
    - tests/fixtures/sample_extraction_drug.json (structure of pre-recorded extraction)
    - tests/fixtures/sample_extraction_contract.json (structure of pre-recorded extraction)
  </read_first>
  <action>
    Create `tests/test_e2e.py` with `@pytest.mark.e2e` tests (per D-05).

    Import from conftest: `from conftest import HAS_SIFTKG, FIXTURES_DIR, PROJECT_ROOT`

    All E2E tests get `@pytest.mark.skipif(not HAS_SIFTKG, reason="sift-kg not installed")` since they need the full pipeline.

    **Test 1: `test_e2e_drug_discovery_pipeline(tmp_path)`** (per D-05):
    Full lifecycle for drug-discovery domain:
    1. Load `sample_extraction_drug.json` from FIXTURES_DIR
    2. Create `tmp_path / "extractions"` directory
    3. Write extraction JSON to `tmp_path / "extractions" / "test_drug_paper.pdf.json"`
    4. Call `cmd_build(str(tmp_path), domain_name="drug-discovery")` — read actual signature first
    5. Assert `(tmp_path / "graph_data.json").exists()`, `f"graph_data.json not created after cmd_build"`
    6. Load graph JSON, assert `len(data["nodes"]) > 0`, `f"Graph has no nodes: {data}"`
    7. Call `label_communities` entry point on tmp_path (if it needs graph_data.json present)
    8. Call `label_epistemic` entry point on tmp_path
    9. Assert `(tmp_path / "claims_layer.json").exists()`, `f"claims_layer.json not created after epistemic analysis"`
    10. Call `cmd_export(str(tmp_path), "json")` — read actual signature first
    11. Verify an export file exists in tmp_path

    **Test 2: `test_e2e_contract_pipeline(tmp_path)`**:
    Same lifecycle for contracts domain:
    1. Load `sample_extraction_contract.json`
    2. Write to `tmp_path / "extractions" / "test_vendor_agreement.pdf.json"`
    3. `cmd_build(str(tmp_path), domain_name="contracts")`
    4. Assert graph_data.json exists with nodes
    5. Run epistemic analysis
    6. Assert claims_layer.json exists
    7. Export and verify

    **Test 3: `test_e2e_pipeline_graph_has_metadata(tmp_path)`**:
    Verify the graph output includes metadata:
    1. Build from drug extraction fixture
    2. Load graph_data.json
    3. Assert `"metadata" in data` or `"nodes" in data and "edges" in data`
    4. Assert all nodes have `"entity_type"` and `"name"` keys

    NOTE: Read actual function signatures from source files before writing. Adapt function calls to match real API. If `cmd_build` uses `sys.argv` parsing instead of function args, you may need to mock `sys.argv` or call the function differently. Check `run_sift.py` carefully.
  </action>
  <verify>
    <automated>cd /Users/umeshbhatt/code/epistract && python -m pytest tests/test_e2e.py -m e2e -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_e2e.py contains `@pytest.mark.e2e` on every test function
    - tests/test_e2e.py contains `def test_e2e_drug_discovery_pipeline`
    - tests/test_e2e.py contains `def test_e2e_contract_pipeline`
    - tests/test_e2e.py contains `skipif(not HAS_SIFTKG`
    - tests/test_e2e.py contains `graph_data.json` assertion
    - tests/test_e2e.py contains `claims_layer.json` assertion
    - `python -m pytest tests/test_e2e.py -m e2e -v` exits 0 (or all tests skipped if sift-kg not installed)
  </acceptance_criteria>
  <done>E2E pipeline test proves full lifecycle from pre-recorded extraction through graph build, epistemic analysis, and export for both domains.</done>
</task>

<task type="auto">
  <name>Task 2: Update Makefile with tiered test targets</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (full file — see existing targets and structure)
  </read_first>
  <action>
    Update Makefile to add tiered test targets (per D-02):

    **Replace** the existing `test` target:
    ```makefile
    test: ## Run unit tests (fast, no external deps)
    	python -m pytest tests/ -m unit -v
    ```

    **Add** new targets after `test`:
    ```makefile
    test-integration: ## Run integration tests (needs sift-kg)
    	python -m pytest tests/ -m integration -v

    test-e2e: ## Run end-to-end pipeline tests (needs sift-kg)
    	python -m pytest tests/ -m e2e -v

    test-all: ## Run all test tiers
    	python -m pytest tests/ -v
    ```

    **Also update** the `lint` and `format` targets to include `core/`:
    ```makefile
    lint: ## Run linter
    	ruff check core/ tests/

    format: ## Format code
    	ruff format core/ tests/
    ```

    Keep all other existing targets unchanged (help, setup, setup-all, clean).
  </action>
  <verify>
    <automated>cd /Users/umeshbhatt/code/epistract && make test 2>&1 | tail -5 && grep -c "test-integration\|test-e2e\|test-all" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - Makefile contains `test:` target with `python -m pytest tests/ -m unit -v`
    - Makefile contains `test-integration:` target with `-m integration`
    - Makefile contains `test-e2e:` target with `-m e2e`
    - Makefile contains `test-all:` target with `python -m pytest tests/ -v`
    - `make test` exits 0 (unit tier passes)
    - Makefile `lint` target includes `core/`
  </acceptance_criteria>
  <done>Makefile has 4 test targets: test (unit), test-integration, test-e2e, test-all. make test runs cleanly.</done>
</task>

</tasks>

<verification>
- `make test` — unit tests pass (TEST-01 final validation)
- `python -m pytest tests/test_e2e.py -m e2e -v` — E2E tests pass or skip (TEST-05)
- `make test-all` — all tiers run
- `make -n test-integration` — shows correct pytest command
</verification>

<success_criteria>
- E2E pipeline test proves full lifecycle for both drug-discovery and contracts domains
- Makefile has 4 tiered test targets matching D-02 specification
- `make test` (unit only) exits 0 on any machine regardless of optional deps
- `make test-all` runs all test tiers
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-framework/07-03-SUMMARY.md`
</output>
