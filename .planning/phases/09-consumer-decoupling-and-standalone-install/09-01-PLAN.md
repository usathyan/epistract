---
phase: 09-consumer-decoupling-and-standalone-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/workbench/template_loader.py
  - examples/workbench/template_schema.py
  - examples/workbench/server.py
  - examples/workbench/system_prompt.py
  - examples/workbench/api_chat.py
  - domains/contracts/workbench/template.yaml
  - domains/drug-discovery/workbench/template.yaml
  - tests/test_workbench.py
autonomous: true
requirements:
  - CONS-01
  - INST-02

must_haves:
  truths:
    - "Workbench server accepts --domain argument and loads domain-specific template"
    - "Workbench falls back to generic defaults when no domain template exists"
    - "System prompt uses domain persona from template instead of hardcoded STA persona"
    - "Pre-built domains (contracts, drug-discovery) each have a workbench/template.yaml"
    - "GET /api/template returns domain template config as JSON"
  artifacts:
    - path: "examples/workbench/template_loader.py"
      provides: "Template discovery and loading with generic fallback"
      exports: ["load_template", "auto_generate_starters"]
    - path: "examples/workbench/template_schema.py"
      provides: "Pydantic WorkbenchTemplate model"
      exports: ["WorkbenchTemplate", "DashboardConfig"]
    - path: "domains/contracts/workbench/template.yaml"
      provides: "Contract domain workbench template"
      contains: "Sample Contract Analysis Workbench"
    - path: "domains/drug-discovery/workbench/template.yaml"
      provides: "Drug discovery domain workbench template"
      contains: "Drug Discovery Knowledge Graph Explorer"
  key_links:
    - from: "examples/workbench/server.py"
      to: "examples/workbench/template_loader.py"
      via: "load_template() at app creation"
      pattern: "load_template"
    - from: "examples/workbench/system_prompt.py"
      to: "app.state.template"
      via: "persona from template dict"
      pattern: "template.*persona"
---

<objective>
Create the domain template system and generalize the workbench backend to load domain-specific configuration from template.yaml files.

Purpose: Enable the workbench to work with any domain (per D-01 through D-04), replacing hardcoded STA content in Python backend files with template-driven configuration. This is the foundation that Plan 02 (frontend) and Plan 03 (telegram bot) build on.

Output: template_loader.py, template_schema.py, two domain template.yaml files, generalized server.py/system_prompt.py/api_chat.py, and template API endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-consumer-decoupling-and-standalone-install/09-CONTEXT.md
@.planning/phases/09-consumer-decoupling-and-standalone-install/09-RESEARCH.md
@.planning/phases/09-consumer-decoupling-and-standalone-install/09-UI-SPEC.md

<interfaces>
<!-- From examples/workbench/data_loader.py — already domain-agnostic -->
class WorkbenchData:
    def __init__(self, output_dir: Path): ...
    def get_nodes(self, entity_type: str | None = None) -> list[dict]: ...
    def get_edges(self) -> list[dict]: ...

<!-- From core/domain_resolver.py — domain discovery pattern -->
DOMAINS_DIR = Path(__file__).parent.parent / "domains"
def resolve_domain(name: str) -> dict: ...
def list_domains() -> list[str]: ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Template schema, loader, and domain template.yaml files</name>
  <files>
    examples/workbench/template_schema.py,
    examples/workbench/template_loader.py,
    domains/contracts/workbench/template.yaml,
    domains/drug-discovery/workbench/template.yaml,
    tests/test_workbench.py
  </files>
  <read_first>
    examples/workbench/system_prompt.py,
    examples/workbench/static/index.html,
    examples/workbench/static/chat.js,
    examples/workbench/static/graph.js,
    core/domain_resolver.py,
    tests/test_workbench.py
  </read_first>
  <behavior>
    - Test: load_template("contracts") returns dict with title="Sample Contract Analysis Workbench"
    - Test: load_template("drug-discovery") returns dict with title containing "Drug Discovery"
    - Test: load_template("nonexistent") returns generic defaults with title="Knowledge Graph Explorer"
    - Test: load_template(None) returns generic defaults
    - Test: auto_generate_starters(["COMPOUND", "GENE", "DISEASE"]) returns 3-4 questions containing "compound"
    - Test: auto_generate_starters([]) returns single fallback question
    - Test: WorkbenchTemplate Pydantic model validates template.yaml content
  </behavior>
  <action>
    1. Create `examples/workbench/template_schema.py` with Pydantic models (per D-02):
       ```python
       class DashboardConfig(BaseModel):
           title: str = "Knowledge Graph Summary"
           subtitle: str = ""

       class WorkbenchTemplate(BaseModel):
           title: str = "Knowledge Graph Explorer"
           subtitle: str = "Explore entities and relationships"
           persona: str = "You are a knowledge graph analyst. Answer questions using the graph data provided."
           placeholder: str = "Ask a question about the knowledge graph..."
           loading_message: str = "Analyzing"
           starter_questions: list[str] = Field(default_factory=list)
           entity_colors: dict[str, str] = Field(default_factory=dict)
           dashboard: DashboardConfig | None = None
       ```

    2. Create `examples/workbench/template_loader.py` (per D-04):
       - `DOMAINS_DIR` resolved via: `Path(os.environ.get("CLAUDE_PLUGIN_ROOT", Path(__file__).resolve().parent.parent.parent)) / "domains"` (handles both repo and plugin install per research pitfall 2)
       - `GENERIC_TEMPLATE` dict with all defaults from WorkbenchTemplate model defaults
       - `load_template(domain_name: str | None = None) -> dict`: scan `DOMAINS_DIR / domain_name / "workbench" / "template.yaml"`, parse with PyYAML, validate with WorkbenchTemplate, merge with defaults. Return plain dict.
       - `auto_generate_starters(entity_types: list[str]) -> list[str]`: generate 3-4 questions from entity type names. If empty, return `["What are the main entities in this knowledge graph?"]`. Use patterns: "What are all the {type} entities and their relationships?", "What conflicts or gaps exist in the data?", "Show me the most connected entities across all types", "How do {type1} entities relate to {type2} entities?"
       - Template loader must be self-contained (import only PyYAML, pathlib, os) -- no imports from core/ (per research pitfall 5)

    3. Create `domains/contracts/workbench/template.yaml` with STA-specific content extracted from current hardcoded files:
       ```yaml
       title: "Sample Contract Analysis Workbench"
       subtitle: "8 contract categories covering 57 documents"
       persona: |
         You are the Sample Contract Analyst -- a senior contract analysis specialist...
         [Full persona from system_prompt.py PERSONA_PROMPT, verbatim]
       placeholder: "Ask about contracts, costs, deadlines, risks..."
       loading_message: "Analyzing contracts"
       starter_questions:
         - "What are the top cross-contract conflicts and risks?"
         - "Walk me through every deadline in order"
         - "Build me a budget summary table"
         [All 13 starter questions from index.html lines 210-223]
       entity_colors:
         PARTY: "#6366f1"
         OBLIGATION: "#f59e0b"
         DEADLINE: "#ef4444"
         COST: "#10b981"
         SERVICE: "#8b5cf6"
         VENUE: "#06b6d4"
         CLAUSE: "#64748b"
         COMMITTEE: "#ec4899"
         PERSON: "#f97316"
         EVENT: "#14b8a6"
         STAGE: "#a855f7"
         ROOM: "#0ea5e9"
       dashboard:
         title: "Sample 2026 Contract Portfolio & Key Financial Commitments"
         subtitle: "8 contract categories covering 57 documents with vendors across venue, catering, hotels, AV, security, decorating, EMS, and event services."
       ```

    4. Create `domains/drug-discovery/workbench/template.yaml`:
       ```yaml
       title: "Drug Discovery Knowledge Graph Explorer"
       subtitle: "Explore biomedical entities and relationships from scientific literature"
       persona: |
         You are a drug discovery knowledge graph analyst. You have reviewed scientific
         literature and built a knowledge graph of compounds, genes, diseases, pathways,
         and their relationships. Answer questions using the graph data provided.
         Cite source documents when referencing specific findings.
         Use markdown tables for comparisons and bullet lists for summaries.
       placeholder: "Ask about compounds, genes, diseases, pathways..."
       loading_message: "Analyzing literature"
       starter_questions:
         - "What compounds target the most genes in this dataset?"
         - "Show me all drug-disease relationships and their evidence strength"
         - "What pathway connections exist between the top compounds?"
         - "Which genes appear in multiple drug interaction contexts?"
       entity_colors:
         COMPOUND: "#6366f1"
         GENE: "#f59e0b"
         DISEASE: "#ef4444"
         PATHWAY: "#10b981"
         MECHANISM: "#8b5cf6"
         CLINICAL_TRIAL: "#06b6d4"
         BIOMARKER: "#64748b"
         SIDE_EFFECT: "#ec4899"
         PROTEIN: "#f97316"
         CELL_LINE: "#14b8a6"
       ```

    5. Add tests to `tests/test_workbench.py` -- add new test functions after existing tests:
       - `test_template_loading_contracts`: load_template("contracts") returns title "Sample Contract Analysis Workbench"
       - `test_template_loading_drug_discovery`: load_template("drug-discovery") returns title containing "Drug Discovery"
       - `test_template_generic_fallback`: load_template("nonexistent") returns title "Knowledge Graph Explorer"
       - `test_template_none_fallback`: load_template(None) returns title "Knowledge Graph Explorer"
       - `test_auto_generate_starters`: auto_generate_starters(["COMPOUND", "GENE"]) returns list with len >= 3
       - `test_auto_generate_starters_empty`: auto_generate_starters([]) returns list with len == 1
       - `test_template_schema_validation`: WorkbenchTemplate(**yaml_dict) succeeds for contracts template
  </action>
  <verify>
    <automated>python -m pytest tests/test_workbench.py -k "template" -v -x</automated>
  </verify>
  <acceptance_criteria>
    - examples/workbench/template_schema.py contains `class WorkbenchTemplate(BaseModel)`
    - examples/workbench/template_schema.py contains `class DashboardConfig(BaseModel)`
    - examples/workbench/template_loader.py contains `def load_template(`
    - examples/workbench/template_loader.py contains `def auto_generate_starters(`
    - examples/workbench/template_loader.py contains `CLAUDE_PLUGIN_ROOT`
    - examples/workbench/template_loader.py does NOT contain `from core` or `import core`
    - domains/contracts/workbench/template.yaml contains `title: "Sample Contract Analysis Workbench"`
    - domains/contracts/workbench/template.yaml contains `PARTY: "#6366f1"`
    - domains/drug-discovery/workbench/template.yaml contains `Drug Discovery`
    - tests/test_workbench.py contains `def test_template_loading_contracts`
    - tests/test_workbench.py contains `def test_template_generic_fallback`
    - `python -m pytest tests/test_workbench.py -k "template" -v -x` exits 0
  </acceptance_criteria>
  <done>Template system loads domain-specific and generic fallback configs. Both pre-built domains have template.yaml files. All template tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize server.py, system_prompt.py, api_chat.py with template API</name>
  <files>
    examples/workbench/server.py,
    examples/workbench/system_prompt.py,
    examples/workbench/api_chat.py,
    tests/test_workbench.py
  </files>
  <read_first>
    examples/workbench/server.py,
    examples/workbench/system_prompt.py,
    examples/workbench/api_chat.py,
    examples/workbench/template_loader.py,
    examples/workbench/template_schema.py,
    tests/test_workbench.py
  </read_first>
  <action>
    1. Update `examples/workbench/server.py`:
       - Change docstring from "Sample Contract Analysis Workbench" to "Epistract Workbench - Domain-agnostic knowledge graph explorer"
       - Add import: `from examples.workbench.template_loader import load_template`
       - Change `create_app(output_dir: Path)` signature to `create_app(output_dir: Path, domain: str | None = None)`
       - After `app.state.data = WorkbenchData(output_dir)`, add: `app.state.template = load_template(domain)`
       - Change FastAPI title from `"Sample Contract Analysis Workbench"` to `app.state.template.get("title", "Knowledge Graph Explorer")`
         (Note: since title is used at FastAPI() init before template is loaded, set title to generic then update: OR pass template title directly. Simplest: compute template first, then pass to FastAPI.)
         Revised approach:
         ```python
         template = load_template(domain)
         app = FastAPI(title=template.get("title", "Knowledge Graph Explorer"))
         app.state.template = template
         ```
       - In root endpoint, change fallback message from "Sample Contract Analysis Workbench" to `app.state.template.get("title", "Knowledge Graph Explorer")`
       - Add new endpoint:
         ```python
         @app.get("/api/template")
         async def get_template():
             return app.state.template
         ```

    2. Update `examples/workbench/system_prompt.py`:
       - Remove the hardcoded `PERSONA_PROMPT` constant entirely
       - Change `build_system_prompt(data: WorkbenchData)` signature to `build_system_prompt(data: WorkbenchData, template: dict)` 
       - First line of function: `parts = [template.get("persona", "You are a knowledge graph analyst. Answer questions using the graph data provided.")]`
       - Change section header from "## CONTRACT KNOWLEDGE GRAPH SUMMARY" to "## KNOWLEDGE GRAPH SUMMARY" (domain-agnostic)
       - Keep all the KG summary logic (nodes, edges, claims, communities, JSON) unchanged -- it is already domain-agnostic
       - `get_matched_source_chunks` remains unchanged (already domain-agnostic)

    3. Update `examples/workbench/api_chat.py`:
       - In `chat()` endpoint: change `system = build_system_prompt(data)` to `system = build_system_prompt(data, request.app.state.template)`
       - Update import to match new signature
       - Change `"X-Title": "STA Contract Workbench"` to `"X-Title": "Epistract Workbench"` in OpenRouter headers

    4. Add test to `tests/test_workbench.py`:
       - `test_template_api_endpoint`: Update the `client` fixture to pass `domain="contracts"` to `create_app()`. Then `GET /api/template` returns 200 with body containing `title` key equal to "Sample Contract Analysis Workbench"
       - `test_template_api_generic`: Create a second client with `domain=None`, verify `GET /api/template` returns title "Knowledge Graph Explorer"
       - Update existing `client` fixture: `create_app(sample_output_dir)` -> `create_app(sample_output_dir, domain="contracts")` so existing tests continue to pass with STA persona
  </action>
  <verify>
    <automated>python -m pytest tests/test_workbench.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - examples/workbench/server.py contains `def create_app(output_dir: Path, domain: str | None = None)`
    - examples/workbench/server.py contains `load_template(domain)`
    - examples/workbench/server.py contains `@app.get("/api/template")`
    - examples/workbench/server.py does NOT contain the string "Sample Contract Analysis Workbench"
    - examples/workbench/system_prompt.py does NOT contain `PERSONA_PROMPT =`
    - examples/workbench/system_prompt.py contains `def build_system_prompt(data: WorkbenchData, template: dict)`
    - examples/workbench/system_prompt.py contains `KNOWLEDGE GRAPH SUMMARY` (not CONTRACT KNOWLEDGE GRAPH)
    - examples/workbench/api_chat.py contains `build_system_prompt(data, request.app.state.template)`
    - examples/workbench/api_chat.py does NOT contain "STA Contract Workbench"
    - tests/test_workbench.py contains `def test_template_api`
    - `python -m pytest tests/test_workbench.py -v -x` exits 0
  </acceptance_criteria>
  <done>Server accepts domain parameter, loads template, serves /api/template endpoint. System prompt uses template persona. No STA-hardcoded strings remain in Python backend files. All existing and new tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_workbench.py -v -x` -- all tests pass
- `python -m pytest tests/test_unit.py -v -x` -- no regressions
- `grep -r "STA" examples/workbench/*.py` -- returns zero results (all STA content moved to template.yaml)
- `python -c "from examples.workbench.template_loader import load_template; t = load_template('contracts'); assert t['title'] == 'Sample Contract Analysis Workbench'"` -- succeeds
- `python -c "from examples.workbench.template_loader import load_template; t = load_template(None); assert t['title'] == 'Knowledge Graph Explorer'"` -- succeeds
</verification>

<success_criteria>
- Template system loads contracts and drug-discovery templates from domains/*/workbench/template.yaml
- Generic fallback works for unknown or missing domains
- Server.py, system_prompt.py, api_chat.py contain zero hardcoded STA references
- /api/template endpoint returns domain template as JSON
- All workbench tests pass (existing + new template tests)
</success_criteria>

<output>
After completion, create `.planning/phases/09-consumer-decoupling-and-standalone-install/09-01-SUMMARY.md`
</output>
