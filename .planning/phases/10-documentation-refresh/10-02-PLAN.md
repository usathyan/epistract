---
phase: 10-documentation-refresh
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - README.md
  - docs/showcases/drug-discovery.md
  - docs/showcases/contracts.md
  - CLAUDE.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "README opens with framework identity, not biomedical framing"
    - "README has dual-path quick-start (pre-built domain vs create-your-own)"
    - "README has How It Works section with inline architecture diagram reference"
    - "README has Pre-built Domains table with entity/relation counts"
    - "README has Showcase section with aggregate stats only (no STA vendor names)"
    - "README has Commands Reference for all /epistract:* commands"
    - "README has no demo video links"
    - "CLAUDE.md reflects framework identity in Project and Architecture sections"
  artifacts:
    - path: "README.md"
      provides: "Framework-first README"
      contains: "Turn any document corpus"
    - path: "docs/showcases/drug-discovery.md"
      provides: "Drug discovery detailed showcase"
      min_lines: 20
    - path: "docs/showcases/contracts.md"
      provides: "Contract analysis detailed showcase"
      min_lines: 20
    - path: "CLAUDE.md"
      provides: "Updated project instructions for AI assistants"
      contains: "cross-domain"
  key_links:
    - from: "README.md"
      to: "docs/showcases/"
      via: "Read the full evaluation links"
      pattern: "docs/showcases/"
    - from: "README.md"
      to: "docs/diagrams/architecture.svg"
      via: "inline image reference"
      pattern: "architecture\\.svg"
---

<objective>
Rewrite README.md as a framework-first document with dual-path quick-start, create showcase detail pages, and update CLAUDE.md for framework identity.

Purpose: README is the project's public face. Must present epistract as a domain-agnostic framework (per D-01), not a biomedical tool. CLAUDE.md ensures AI assistants understand the framework identity (per D-19).
Output: Rewritten README.md, 2 showcase files in docs/showcases/, updated CLAUDE.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-documentation-refresh/10-CONTEXT.md
@.planning/phases/10-documentation-refresh/10-01-SUMMARY.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md as framework-first document with showcases</name>
  <files>README.md, docs/showcases/drug-discovery.md, docs/showcases/contracts.md</files>
  <read_first>
    - README.md (current 514-line biomedical README — full content needed to preserve useful material)
    - domains/contracts/domain.yaml (for entity/relation counts in Pre-built Domains table)
    - domains/drug-discovery/domain.yaml (for entity/relation counts in Pre-built Domains table)
    - docs/diagrams/architecture.mmd (verify diagram exists from Plan 01)
    - .planning/phases/10-documentation-refresh/10-CONTEXT.md (all D-01 through D-08 decisions)
  </read_first>
  <action>
Create docs/showcases/ directory. Write three files:

**README.md — Full rewrite following this structure (per D-01 through D-08):**

```markdown
# Epistract

**Turn any document corpus into a structured knowledge graph.**

Epistract is a domain-agnostic framework ... [framework pitch, 2-3 sentences]
... two-layer knowledge graph: brute-force entity/relation extraction, then domain-specific epistemic analysis (conflicts, gaps, risks, confidence).

## Quick Start

### Path A: Use a Pre-Built Domain

Three commands to first graph (per D-02):
1. `/epistract:setup` — install framework
2. `/epistract:ingest --domain contracts --input ./my-docs/` — parse and extract
3. `/epistract:view` — open interactive graph

### Path B: Create Your Own Domain

Five steps (per D-02):
1. `/epistract:setup`
2. `/epistract:domain --input ./sample-docs/` — wizard analyzes docs, proposes schema
3. Review and confirm generated domain package
4. `/epistract:ingest --domain your-domain --input ./corpus/`
5. `/epistract:view`

## How It Works

[Per D-05: 1-2 paragraphs explaining two-layer KG + domain pluggability]
[Inline reference to architecture diagram: ![Architecture](docs/diagrams/architecture.svg)]

### Two-Layer Knowledge Graph
- **Layer 1 — Brute Facts:** Entities and relations extracted from documents via LLM agents, constrained by domain schema
- **Layer 2 — Epistemic Analysis:** Domain-specific rules detect conflicts, gaps, confidence levels, and risks across the graph

### Domain Pluggability
New domains are added by creating a configuration package (domain.yaml + SKILL.md + epistemic.py) — no pipeline code changes. Use the wizard or create manually.

## Pre-built Domains

[Per D-06: Table format]
| Domain | Entity Types | Relation Types | Document Formats | Description |
|--------|-------------|----------------|------------------|-------------|
| drug-discovery | 17 | 30 | PDF, DOCX, HTML, TXT | Biomedical literature analysis |
| contracts | 11 | 11 | PDF, XLS, EML | Event/vendor contract analysis |

## Showcase

[Per D-03: 2-3 paragraphs each, aggregate stats only for contracts]

### Drug Discovery Literature Analysis
[Summary of biomedical scenarios 1-6: multi-document extraction, molecular validation, epistemic analysis. Link to detailed write-up.]
[Read the full evaluation →](docs/showcases/drug-discovery.md)

### Event Contract Management
Applied to 62 event planning contracts. Extracted 341 nodes across 11 entity types with 663 edges across 11 relation types. Epistemic analysis detected 53 cross-contract conflicts. No vendor names or dollar amounts — aggregate stats only per D-16.
[Read the full evaluation →](docs/showcases/contracts.md)

## Commands Reference

[Per D-07: All /epistract:* commands with one-line descriptions]
| Command | Description |
|---------|-------------|
| `/epistract:setup` | Install framework and dependencies |
| `/epistract:ingest` | Parse documents and extract entities/relations |
| `/epistract:build` | Rebuild graph from existing extractions |
| `/epistract:query` | Search/filter the knowledge graph |
| `/epistract:export` | Export graph (GraphML, CSV, SQLite, JSON) |
| `/epistract:view` | Open interactive graph visualization |
| `/epistract:validate` | Run molecular validation on extractions |
| `/epistract:epistemic` | Run epistemic analysis on graph |
| `/epistract:domain` | Create new domain via wizard |
| `/epistract:dashboard` | Launch web dashboard for domain |
| `/epistract:ask` | Chat with graph via natural language |

## The Name

[Preserve existing etymology section from current README — "epistemic" + "extract"]

## Contributing / Development

[Per D-08:]
### Setup
git clone ... && cd epistract
uv pip install -e ".[dev]"

### Testing
make test          # unit tests
make test-integ    # integration tests

### Project Structure
core/              # Domain-agnostic pipeline engine
domains/           # Pluggable domain configurations
examples/          # Consumer applications (workbench, telegram bot)
tests/             # Test suite
docs/              # Documentation and diagrams

See [CLAUDE.md](CLAUDE.md) for AI-assisted development conventions.

## License

[Keep existing license section]
```

Do NOT include any demo video links (per D-04). Do NOT include biomedical-specific language in the pitch or How It Works sections. Drug discovery appears only in the Pre-built Domains table and Showcase section.

**docs/showcases/drug-discovery.md:**
Move the detailed drug discovery scenario content from current README into this file. Include scenarios 1-6 summary with methodology, results, and molecular validation details. This is where the detailed biomedical content lives.

**docs/showcases/contracts.md:**
Write detailed contract analysis showcase. Include: methodology (62 contracts, multi-format parsing), extraction results (341 nodes, 663 edges, 11 entity types, 11 relation types), epistemic analysis (53 conflicts detected, gap analysis, risk scoring). Aggregate stats only per D-16 — no vendor names, dollar amounts, or specific contract terms.
  </action>
  <verify>
    <automated>grep -q "Turn any document corpus" README.md && grep -q "docs/showcases/" README.md && ! grep -qi "youtube\|demo.*video\|7mHbdb0nn3Y" README.md && grep -c "skills/" README.md | grep -q "^0$" && echo "README OK" || echo "README ISSUES"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "Turn any document corpus into a structured knowledge graph" (per D-01)
    - README.md contains "## Quick Start" with "Path A" and "Path B" subsections (per D-02)
    - README.md contains "## How It Works" (per D-05)
    - README.md contains "## Pre-built Domains" with a markdown table (per D-06)
    - README.md contains "## Showcase" (per D-03)
    - README.md contains "## Commands Reference" with `/epistract:domain` listed (per D-07)
    - README.md contains "## Contributing" (per D-08)
    - README.md does NOT contain "youtube", "demo video", or "7mHbdb0nn3Y" (per D-04)
    - README.md does NOT contain "skills/" path references (stale V1 paths)
    - README.md does NOT contain "aramark", "freeman", or other STA vendor names (per D-16)
    - README.md contains link to "docs/showcases/drug-discovery.md"
    - README.md contains link to "docs/showcases/contracts.md"
    - docs/showcases/drug-discovery.md exists with at least 20 lines
    - docs/showcases/contracts.md exists with at least 20 lines and contains "62" and "341" and "53 conflicts"
    - docs/showcases/contracts.md does NOT contain "aramark", "freeman", or dollar amounts
  </acceptance_criteria>
  <done>README.md fully rewritten as framework-first document. Showcase detail pages created. No stale paths, no STA data leakage, no demo video links.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md for framework identity</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current 268-line project instructions — full read needed)
    - .planning/phases/10-documentation-refresh/10-CONTEXT.md (D-19 decision)
    - domains/contracts/domain.yaml (verify entity/relation counts)
    - domains/drug-discovery/domain.yaml (verify entity/relation counts)
  </read_first>
  <action>
Per D-19: Update CLAUDE.md Project section, Architecture section, and conventions to reflect framework identity.

**Project section changes:**
- Replace opening description. Current: "Epistract Cross-Domain Knowledge Graph Framework ... evolves from a biomedical-specific literature extraction tool..."
- New: Lead with framework identity. "Epistract is a domain-agnostic knowledge graph framework. Plug in a domain schema (YAML + extraction prompts + epistemic rules), point at a document corpus, and get a structured knowledge graph with epistemic analysis layer."
- Keep Constraints subsection but update: remove "first cross-domain application" framing. State the two pre-built domains (drug-discovery, contracts) as examples, not as the project identity.
- Update "Core Value" to: "Extract knowledge, not information. Any corpus, any domain — plug in a schema, get a knowledge graph with epistemic layer."

**Architecture section changes:**
- Update "Pattern Overview" to describe three-layer architecture: core/ (pipeline engine), domains/ (pluggable configs), examples/ (consumers)
- Update "Layers" to reflect actual codebase structure:
  - Core layer: core/ directory with domain_resolver.py, ingest_documents.py, build_extraction.py, run_sift.py, epistemic.py, domain_wizard.py
  - Domain layer: domains/ directory with drug-discovery/ and contracts/
  - Consumer layer: examples/ with workbench/ and telegram_bot/
- Update "Entry Points" to include /epistract:domain, /epistract:dashboard, /epistract:ask
- Remove stale references to skills/{name}-extraction/ paths — replace with domains/{name}/ paths
- Remove references to scripts/ that have been moved to core/

**Conventions section — preserve as-is** unless paths reference stale locations (skills/, scripts/).

**Technology Stack section:**
- Update if any tool versions or dependencies changed in V2
- Add domain_wizard.py to the sift-kg wrapper description

Do NOT change:
- Developer Profile section
- GSD Workflow Enforcement section
- Language & Tooling conventions (ruff, pytest, etc.)
- Testing Conventions
  </action>
  <verify>
    <automated>grep -q "domain-agnostic" CLAUDE.md && ! grep -q "evolves from a biomedical" CLAUDE.md && grep -q "domains/" CLAUDE.md && echo "CLAUDE.md OK" || echo "CLAUDE.md ISSUES"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "domain-agnostic" in the Project section
    - CLAUDE.md does NOT contain "evolves from a biomedical-specific literature extraction tool"
    - CLAUDE.md contains "core/" and "domains/" and "examples/" in Architecture section
    - CLAUDE.md contains "/epistract:domain" in Entry Points
    - CLAUDE.md contains "/epistract:dashboard" in Entry Points
    - CLAUDE.md does NOT contain "skills/drug-discovery-extraction/" (stale V1 path) outside of historical references
    - CLAUDE.md GSD Workflow Enforcement section is unchanged
    - CLAUDE.md Developer Profile section is unchanged
  </acceptance_criteria>
  <done>CLAUDE.md updated to reflect framework identity. Project, Architecture, and Entry Points sections reframed. Conventions and workflow sections preserved.</done>
</task>

</tasks>

<verification>
```bash
# README framework identity
grep "Turn any document corpus" README.md

# README dual-path quick-start
grep -c "Path A\|Path B" README.md

# No demo video
grep -i "youtube\|demo.*video" README.md && echo "FAIL" || echo "PASS"

# No stale paths
grep -c "skills/" README.md | grep "^0$" && echo "PASS" || echo "FAIL"

# No STA leakage
grep -i "aramark\|freeman" README.md docs/showcases/*.md && echo "FAIL" || echo "PASS"

# Showcases exist
test -f docs/showcases/drug-discovery.md && test -f docs/showcases/contracts.md && echo "PASS" || echo "FAIL"

# CLAUDE.md framework identity
grep "domain-agnostic" CLAUDE.md
```
</verification>

<success_criteria>
- README.md is framework-first with all D-01 through D-08 sections present
- No biomedical framing in pitch, How It Works, or quick-start sections
- No demo video links anywhere in README
- No STA vendor names or dollar amounts in any documentation
- Two showcase detail files exist in docs/showcases/
- CLAUDE.md reflects framework identity in Project and Architecture sections
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-refresh/10-02-SUMMARY.md`
</output>
