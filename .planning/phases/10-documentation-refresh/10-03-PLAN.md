---
phase: 10-documentation-refresh
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - docs/ADDING-DOMAINS.md
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "Domain guide leads with wizard workflow (5-step happy path)"
    - "Domain guide has manual creation section with annotated schema reference"
    - "Domain guide references domains/ paths, not skills/ paths"
    - "Domain guide includes examples from both existing domains (drug-discovery and contracts)"
    - "Domain guide references domain package anatomy diagram"
  artifacts:
    - path: "docs/ADDING-DOMAINS.md"
      provides: "Domain developer guide"
      contains: "/epistract:domain"
      min_lines: 150
  key_links:
    - from: "docs/ADDING-DOMAINS.md"
      to: "docs/diagrams/domain-package.svg"
      via: "inline image reference"
      pattern: "domain-package"
    - from: "docs/ADDING-DOMAINS.md"
      to: "domains/contracts/domain.yaml"
      via: "annotated example reference"
      pattern: "domains/contracts"
    - from: "docs/ADDING-DOMAINS.md"
      to: "domains/drug-discovery/domain.yaml"
      via: "annotated example reference"
      pattern: "domains/drug-discovery"
---

<objective>
Rewrite docs/ADDING-DOMAINS.md as a wizard-first domain developer guide with manual creation reference.

Purpose: This is the primary onboarding document for domain developers. Per D-11 and D-12, it leads with the wizard workflow and provides a comprehensive manual reference with annotated examples from both existing domains.
Output: Rewritten docs/ADDING-DOMAINS.md (~200-300 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-documentation-refresh/10-CONTEXT.md
@.planning/phases/10-documentation-refresh/10-01-SUMMARY.md
@docs/ADDING-DOMAINS.md
@domains/contracts/domain.yaml
@domains/drug-discovery/domain.yaml
@domains/contracts/SKILL.md
@domains/contracts/epistemic.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite domain developer guide with wizard-first structure</name>
  <files>docs/ADDING-DOMAINS.md</files>
  <read_first>
    - docs/ADDING-DOMAINS.md (current 323-line guide with stale skills/ paths)
    - domains/contracts/domain.yaml (contract schema — annotated example for guide)
    - domains/drug-discovery/domain.yaml (drug-discovery schema — annotated example for guide)
    - domains/contracts/SKILL.md (extraction prompt example)
    - domains/contracts/epistemic.py (epistemic rules example — function signatures)
    - domains/drug-discovery/epistemic.py (drug-discovery epistemic rules — function signatures)
    - domains/contracts/workbench/template.yaml (workbench template example)
    - core/domain_wizard.py (wizard API for accurate command documentation)
    - core/domain_resolver.py (resolver API for how domains are discovered)
    - docs/diagrams/domain-package.mmd (verify diagram exists from Plan 01)
  </read_first>
  <action>
Rewrite docs/ADDING-DOMAINS.md following the structure from D-11, D-12, and the research recommendation:

```markdown
# Adding a Domain to Epistract

## Quick Start: Domain Wizard

The fastest path to a working domain. Five steps:

### Step 1: Gather Sample Documents
[Explain: 3-5 representative documents from the target corpus]

### Step 2: Run the Wizard
`/epistract:domain --input ./sample-docs/`
[Explain: wizard reads documents, proposes entity types and relation types, generates domain.yaml + SKILL.md + epistemic.py]

### Step 3: Review the Generated Schema
[Explain: wizard outputs to domains/your-domain/. Review domain.yaml for entity/relation types. Adjust as needed.]

### Step 4: Test with Full Corpus
`/epistract:ingest --domain your-domain --input ./full-corpus/`
[Explain: run extraction on full document set, check entity/relation quality]

### Step 5: Explore Your Graph
`/epistract:view`
[Explain: interactive visualization, run epistemic analysis, export]

## What Gets Generated

[Reference domain-package anatomy diagram: ![Domain Package](diagrams/domain-package.svg)]

A domain package directory contains:

| File | Purpose | Required |
|------|---------|----------|
| domain.yaml | Entity types, relation types, aliases | Yes |
| SKILL.md | LLM extraction prompt with domain knowledge | Yes |
| epistemic.py | Domain-specific analysis rules (conflicts, gaps, risks) | Yes |
| references/ | Ontology references, nomenclature guides | Optional |
| workbench/ | Dashboard customization (template.yaml) | Optional |

## Manual Domain Creation

For power users who want full control or need to customize beyond what the wizard generates.

### domain.yaml Reference

[Per D-12: Annotated field-by-field reference using both domains as examples]

```yaml
# Domain metadata
name: "your-domain"          # Used in --domain flag
description: "..."           # Human-readable description
version: "1.0"

# Entity types — what to extract from documents
entity_types:
  ENTITY_NAME:               # SCREAMING_SNAKE_CASE
    description: "..."       # Guides LLM extraction
    attributes:              # Optional structured fields
      - name: "field_name"
        type: "string"

# Relation types — how entities connect
relation_types:
  RELATION_NAME:
    description: "..."
    source_types: [...]      # Which entity types can be source
    target_types: [...]      # Which entity types can be target

# Aliases for domain resolution
aliases: ["alias1", "alias2"]
```

**Drug Discovery example** (17 entity types, 30 relation types):
[Show 3-4 representative entity types from domains/drug-discovery/domain.yaml with annotations explaining why they exist and how they're used]

**Contracts example** (11 entity types, 11 relation types):
[Show 3-4 representative entity types from domains/contracts/domain.yaml with annotations]

### SKILL.md Guide

[Explain the extraction prompt structure:]
- Role definition for the LLM agent
- Domain context (what documents look like, what to extract)
- Entity type descriptions with examples
- Relation type descriptions with examples
- Output format specification (DocumentExtraction JSON)
- Confidence scoring guidelines

[Show abbreviated examples from both domains' SKILL.md files]

### epistemic.py Reference

[Per D-12: Function signature reference]

The epistemic module must export an `analyze` function:

```python
def analyze(graph_data: dict, extractions: list[dict]) -> dict:
    """Run domain-specific epistemic analysis.
    
    Args:
        graph_data: NetworkX graph as dict (nodes, edges)
        extractions: List of DocumentExtraction dicts
    
    Returns:
        dict with keys: conflicts, gaps, risks, summary
    """
```

[Show how drug-discovery epistemic.py handles molecular validation vs how contracts epistemic.py handles cross-contract conflicts — contrast to illustrate domain-specific patterns]

### Workbench Customization (Optional)

[Brief section on workbench/template.yaml for dashboard customization]
[Show contracts template.yaml as example: entity_colors, persona_prompt, dashboard HTML]

## Testing Your Domain

```bash
# Validate domain schema
python -c "from core.domain_resolver import resolve_domain; print(resolve_domain('your-domain'))"

# Run extraction on test documents  
/epistract:ingest --domain your-domain --input ./test-docs/

# Verify graph output
/epistract:query --domain your-domain --type ENTITY_NAME

# Run tests
python -m pytest tests/ -k "your_domain" -v
```

## Tips

- Start with 5-10 entity types — you can always add more
- Use the wizard output as a starting point, then customize
- Look at both existing domains for patterns: drug-discovery for complex extraction, contracts for simpler but effective schemas
- Epistemic rules are the differentiator: every domain should define what conflicts, gaps, and risks mean in its context
```

Every path reference must use `domains/{name}/` not `skills/{name}-extraction/`. Verify by grepping the final file for "skills/" — must return 0 matches.

Include actual code snippets from the domain packages (read them in read_first). Do not invent fake YAML or Python — use real examples from the codebase.
  </action>
  <verify>
    <automated>grep -c "skills/" docs/ADDING-DOMAINS.md | grep -q "^0$" && grep -q "/epistract:domain" docs/ADDING-DOMAINS.md && grep -q "domains/contracts" docs/ADDING-DOMAINS.md && grep -q "domains/drug-discovery" docs/ADDING-DOMAINS.md && echo "Guide OK" || echo "Guide ISSUES"</automated>
  </verify>
  <acceptance_criteria>
    - docs/ADDING-DOMAINS.md contains "## Quick Start: Domain Wizard" as first major section (per D-11)
    - docs/ADDING-DOMAINS.md contains "/epistract:domain" command reference
    - docs/ADDING-DOMAINS.md contains "## Manual Domain Creation" section (per D-12)
    - docs/ADDING-DOMAINS.md contains "domain.yaml Reference" with annotated YAML
    - docs/ADDING-DOMAINS.md contains "SKILL.md" guide section
    - docs/ADDING-DOMAINS.md contains "epistemic.py" reference with function signature `def analyze(`
    - docs/ADDING-DOMAINS.md references "domains/contracts" (not "skills/contract-extraction")
    - docs/ADDING-DOMAINS.md references "domains/drug-discovery" (not "skills/drug-discovery-extraction")
    - docs/ADDING-DOMAINS.md contains "domain-package" diagram reference
    - `grep -c "skills/" docs/ADDING-DOMAINS.md` returns 0
    - docs/ADDING-DOMAINS.md has at least 150 lines
  </acceptance_criteria>
  <done>Domain developer guide rewritten with wizard-first structure, manual reference with annotated examples from both domains, all paths updated to domains/ structure.</done>
</task>

</tasks>

<verification>
```bash
# No stale paths
grep -c "skills/" docs/ADDING-DOMAINS.md

# Wizard-first structure
head -30 docs/ADDING-DOMAINS.md | grep -i "wizard"

# Both domains referenced
grep "domains/contracts" docs/ADDING-DOMAINS.md
grep "domains/drug-discovery" docs/ADDING-DOMAINS.md

# Domain package diagram reference
grep "domain-package" docs/ADDING-DOMAINS.md

# Function signature present
grep "def analyze" docs/ADDING-DOMAINS.md

# Line count
wc -l docs/ADDING-DOMAINS.md
```
</verification>

<success_criteria>
- docs/ADDING-DOMAINS.md is wizard-first with 5-step happy path
- Manual creation section has annotated schema reference from both domains
- All path references use domains/{name}/ structure
- No stale skills/ path references
- Guide is self-contained: a domain developer can follow it without reading other docs
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-refresh/10-03-SUMMARY.md`
</output>
