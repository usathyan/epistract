---
phase: 11-end-to-end-scenario-validation
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - docs/showcases/drug-discovery-screenshots.md
  - docs/showcases/contracts-screenshots.md
  - tests/baselines/v2/s01_picalm.json
  - tests/baselines/v2/s02_kras.json
  - tests/baselines/v2/s03_rare_disease.json
  - tests/baselines/v2/s04_immunooncology.json
  - tests/baselines/v2/s05_cardiovascular.json
  - tests/baselines/v2/s06_glp1.json
  - tests/baselines/v2/contracts.json
autonomous: false
requirements: [E2E-01, E2E-02, E2E-03, E2E-04, E2E-06]

must_haves:
  truths:
    - "All 6 drug discovery scenarios produce graphs with >=80% of V1 node/edge counts"
    - "Contract scenario produces graph with >=80% of V1 node/edge counts (if STA data available)"
    - "All extractions ran through /epistract:* plugin commands"
    - "Epistemic layers produce correct output (molecular validation + conflict detection)"
    - "V2 baselines saved as new canonical baselines after validation passes"
    - "Graph visualizations are demonstration-ready for both domains"
  artifacts:
    - path: "tests/baselines/v2/s01_picalm.json"
      provides: "V2 canonical baseline for scenario 1"
      contains: '"version": "v2"'
    - path: "docs/showcases/"
      provides: "Demonstration screenshots"
  key_links:
    - from: "tests/baselines/v2/"
      to: "tests/regression/run_regression.py"
      via: "--baselines flag"
      pattern: "tests/baselines/v2"
---

<objective>
Run all 7 scenarios through the V2 plugin pipeline, validate output against V1 baselines, save V2 baselines, and capture demonstration screenshots.

Purpose: Proves the V2 framework produces correct knowledge graphs end-to-end (per D-01 through D-08).
Output: V2 baselines, validation report, demonstration screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-end-to-end-scenario-validation/11-CONTEXT.md
@.planning/phases/11-end-to-end-scenario-validation/11-RESEARCH.md
@.planning/phases/11-end-to-end-scenario-validation/11-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run all scenarios through V2 plugin pipeline</name>
  <files>tests/corpora/01_picalm_alzheimers/output/graph_data.json</files>
  <action>
    The regression infrastructure from Plan 01 is ready. Now the user must run all scenarios through the V2 plugin to generate fresh extraction output.

    Per D-04, ALL validation runs through `/epistract:*` plugin commands only (not raw Python scripts). Per D-01, all 6 drug discovery scenarios must be re-run. Per D-02, the contract scenario re-runs the full STA corpus.

    **Drug Discovery Scenarios (D-01):**
    For each scenario (1-6), run the following through the epistract plugin:
    1. `/epistract:ingest` pointing at `tests/corpora/01_picalm_alzheimers/docs/` (and similarly for 02-06)
    2. `/epistract:build` to construct the knowledge graph
    3. `/epistract:epistemic` to run epistemic analysis
    4. `/epistract:view` to generate graph visualization
    5. `/epistract:validate` to run molecular validation (drug discovery only)

    Store output in `tests/corpora/XX_scenario_name/output-v2/` to avoid overwriting V1 baselines.

    **Contract Scenario (D-02):**
    1. `/epistract:ingest` pointing at the STA contracts corpus
    2. `/epistract:build --domain contracts`
    3. `/epistract:epistemic --domain contracts`
    4. `/epistract:view`

    Store output in `sample-output-v2/` or similar.

    **After all runs complete:**
    Run: `python tests/regression/run_regression.py --baselines tests/baselines/v1/ --output-dir tests/corpora/` (adjust output-dir if V2 output is in different location)

    **Epistemic Validation (D-05):**
    - Drug discovery: Check that `claims_layer.json` exists and contains molecular validation entries with SMILES/sequences flagged
    - Contracts: Check that conflict count >=40 (baseline 53), obligations and deadlines detected

    **Expected outcomes per D-03:** All scenarios produce >=80% of V1 entity/relation counts.
  </action>
  <verify>User confirms "validated" with regression report output, or "validated-no-contracts" if STA data unavailable</verify>
  <done>All scenarios run through V2 plugin pipeline. Regression report shows >=80% of V1 baseline counts for all scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Save V2 baselines and capture demonstration artifacts</name>
  <files>
    tests/baselines/v2/s01_picalm.json,
    tests/baselines/v2/s02_kras.json,
    tests/baselines/v2/s03_rare_disease.json,
    tests/baselines/v2/s04_immunooncology.json,
    tests/baselines/v2/s05_cardiovascular.json,
    tests/baselines/v2/s06_glp1.json,
    tests/baselines/v2/contracts.json
  </files>
  <read_first>
    tests/regression/run_regression.py,
    tests/baselines/v1/s01_picalm.json
  </read_first>
  <action>
    Per D-08: After validation passes threshold, save V2 counts as new canonical baselines.

    1. Run `python tests/regression/run_regression.py --update-baselines` to generate V2 baseline JSON files in `tests/baselines/v2/`.

    2. Verify V2 baselines were created: `ls tests/baselines/v2/`

    3. Run regression against V2 baselines to confirm they match current output (should be 100% pass):
       `python tests/regression/run_regression.py --baselines tests/baselines/v2/`

    4. Per D-06 and D-07: Capture graph visualization screenshots for demonstration.
       - For each drug discovery scenario: open the `graph.html` from each scenario's output in a browser, capture a screenshot, save to `docs/showcases/`
       - For contracts: same process
       - If Playwright is available, automate screenshot capture. If not, note in summary that screenshots need manual capture.

    5. Create `docs/showcases/drug-discovery-screenshots.md` listing all 6 drug discovery graph screenshots with scenario labels and node/edge counts.

    6. Create `docs/showcases/contracts-screenshots.md` listing the contracts graph screenshot with node/edge/conflict counts.

    Note: If V2 output directories differ from V1 locations, update the regression runner's output-dir mapping accordingly. The `--update-baselines` flag should handle this based on where the scenario output actually is.
  </action>
  <verify>
    <automated>python tests/regression/run_regression.py --baselines tests/baselines/v2/ 2>&1; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - tests/baselines/v2/ directory exists with at least 6 JSON files
    - Each V2 baseline JSON contains `"version": "v2"`
    - `python tests/regression/run_regression.py --baselines tests/baselines/v2/` exits 0
    - docs/showcases/drug-discovery-screenshots.md exists
    - docs/showcases/contracts-screenshots.md exists (or noted as skipped if no STA data)
  </acceptance_criteria>
  <done>V2 baselines saved as canonical. Regression suite passes against V2 baselines. Demonstration artifacts captured in docs/showcases/.</done>
</task>

</tasks>

<verification>
- `python tests/regression/run_regression.py --baselines tests/baselines/v2/` exits 0
- All V2 baseline files exist in tests/baselines/v2/
- Demonstration screenshots or documentation in docs/showcases/
</verification>

<success_criteria>
1. All 6 drug discovery scenarios validated at >=80% of V1 baseline counts
2. Contract scenario validated (or skipped with documented reason)
3. V2 baselines saved and regression passes against them
4. Demonstration artifacts in docs/showcases/
</success_criteria>

<output>
After completion, create `.planning/phases/11-end-to-end-scenario-validation/11-03-SUMMARY.md`
</output>
