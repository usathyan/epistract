---
phase: 20-readme-pipeline-capacity-and-limits
plan: 01
type: execute
wave: 1
autonomous: true
requirements:
  - FIDL-09
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
---

# Phase 20 Plan 1: README "Pipeline Capacity & Limits" Section (FIDL-09 close-out)

Land the FIDL-09 README section documenting the post-v3.0 (Phases 12–19) pipeline state. Docs-only phase. Single-plan close-out of the v3.0 "Graph Fidelity & Honest Limits" milestone.

## Objective

Insert a concise, grep-verifiable `## Pipeline Capacity & Limits` section into `README.md` between `## How It Works` and `## Pre-built Domains`, flip FIDL-09 `Pending → Complete`, and record the milestone close-out.

## Source of truth

- `docs/known-limitations.md` — every numeric claim in the new section must be sourced here (FIDL-05, FIDL-06, FIDL-07, FIDL-08 sections).
- Codebase grep-verifiable constants (`core/chunk_document.OVERLAP_SENTENCES`, `core/domain_wizard.EXCERPT_CHARS`, `core/label_epistemic.HEDGING_PATTERNS`, `core/ingest_documents._supported_extensions`).

## Commits (2 atomic after this plan doc, all `--no-verify`)

1. **Task 2 — Section + flip:** Insert `## Pipeline Capacity & Limits` directly before `## Pre-built Domains`. Four subsections (Document Ingestion, Wizard Schema Discovery, Extraction & Graph Build, Epistemic Layer), each a `Feature | Value | Notes` table plus a "Known limits" line linking to `docs/known-limitations.md`. Same commit flips FIDL-09 `Pending → Complete` + `[ ] → [x]` and updates the REQUIREMENTS footer to `2026-04-22 — FIDL-09 Phase 20 complete (README Pipeline Capacity & Limits)`.
2. **Task 3 — Summary:** Write `20-01-SUMMARY.md` noting v3.0 milestone complete.

## Verification

- `grep -c "^## Pipeline Capacity & Limits" README.md` == 1
- `grep -c "docs/known-limitations.md" README.md` ≥ 2
- `awk '/^## Pipeline Capacity & Limits/,/^## Pre-built Domains/' README.md | wc -l` ≤ 160
- `grep -c "^| FIDL-09 |" .planning/REQUIREMENTS.md` == 1
- `grep -c "^- \[x\] \*\*FIDL-09\*\*" .planning/REQUIREMENTS.md` == 1

## Success criteria

- README section is additive only — no existing README content modified.
- Every number in the section grep-verifiable in code or sourced from `docs/known-limitations.md`.
- Section length ≤ 150 lines of markdown.
- FIDL-09 flipped to Complete in a single commit with the README insertion.
- v3.0 milestone status ready to close after verification.
