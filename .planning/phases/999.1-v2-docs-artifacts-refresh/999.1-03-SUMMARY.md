---
phase: 999.1
plan: "03"
subsystem: docs
tags: [documentation, enrichment, domain-guide, adding-domains]
dependency_graph:
  requires: [999.1-01]
  provides: [enrichment-section-in-adding-domains]
  affects: [docs/ADDING-DOMAINS.md]
tech_stack:
  added: []
  patterns: [non-blocking-fetch-pattern, exponential-backoff, enrichment-report-schema]
key_files:
  modified:
    - docs/ADDING-DOMAINS.md
decisions:
  - "Inserted section exactly between Workbench Customization closing --- and ## Testing Your Domain"
  - "All code examples use placeholder names (your-domain, your-entity-type, YOUR_URL) — no secrets"
  - "PubChem ConnectivitySMILES quirk documented twice per T-999.1-12 mitigation requirement"
metrics:
  duration: "2min"
  completed: "2026-04-18"
  tasks_completed: 1
  files_modified: 1
---

# Phase 999.1 Plan 03: Domain Enrichment Section in ADDING-DOMAINS.md Summary

Added a full `## Domain Enrichment (Optional)` section to `docs/ADDING-DOMAINS.md` documenting the `enrich.py` extension pattern with `domains/clinicaltrials/enrich.py` as the canonical worked example.

## What Was Built

A new top-level section inserted into `docs/ADDING-DOMAINS.md` between the "Workbench Customization (Optional)" subsection's closing `---` separator and the existing `## Testing Your Domain` heading.

The section covers six subsections:

1. **When to Use Enrichment** — when to add `enrich.py` (stable external IDs, public machine-queryable APIs, non-blocking required) and when not to (extraction-prompt data, unreliable APIs, auth-gated APIs)
2. **The `enrich_graph()` Contract** — exact function signature with canonical skeleton code showing sift-kg/raw-JSON dual-path load, per-node enrichment loop, persistence, and report writing
3. **The `_fetch_*` Non-Blocking Pattern** — helper function pattern returning `None` on any failure; includes `_fetch_pubchem` example with 429 exponential backoff and PubChem `ConnectivitySMILES` quirk documented
4. **The `_enrichment_report.json` Output** — JSON schema with `generated_at`, `domain`, per-entity-type counts (`total`/`enriched`/`not_found`/`failed`), and `hit_rate` block
5. **Wiring the `--enrich` Flag** — how to update Step 5.5 domain-gate check in `commands/ingest.md`, non-blocking pipeline contract
6. **ClinicalTrials Reference Implementation** — table of CT.gov + PubChem API targets and attached attributes; rate limit documentation; PubChem `ConnectivitySMILES` quirk repeated per threat model mitigation T-999.1-12

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Insert Domain Enrichment (Optional) section | 4a55dd7 | docs/ADDING-DOMAINS.md |

## Verification Results

All automated assertions passed:
- Section header count: 1
- All 6 subsections present
- `enrich_graph()` signature present
- `_fetch_your_api` non-blocking example present
- `domains/clinicaltrials/enrich.py` cited 4 times
- `ConnectivitySMILES` mentioned 4 times (quirk properly repeated)
- `CanonicalSMILES` (request side) mentioned
- `5 req/sec` rate limit documented
- `exponential backoff` mentioned 3 times
- `--enrich` flag documented
- `Step 5.5` referenced 2 times
- `_enrichment_report.json` mentioned 3 times
- `"hit_rate"` schema key present
- Section order: Workbench Customization < Domain Enrichment < Testing Your Domain
- All preserved sections intact: Quick Start, What Gets Generated, Manual Domain Creation, Testing Your Domain, Tips
- Drug Discovery Example (17 entity types) and Contracts Example (9 entity types) preserved

## Deviations from Plan

None — plan executed exactly as written. The single task inserted the prescribed content at the prescribed location with all required subsections and the exact function signatures from the `<interfaces>` block.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. The new section documents public APIs (ClinicalTrials.gov, PubChem) but does not wire them — it is prose documentation only. Code examples use placeholder `YOUR_URL` — no hardcoded API endpoints in the doc. T-999.1-09 (insertion is additive, preserved sections verified), T-999.1-10 (no secrets in examples), T-999.1-11 (rate-limit backoff pattern documented), T-999.1-12 (ConnectivitySMILES quirk repeated twice) — all mitigated.

## Self-Check: PASSED

- `docs/ADDING-DOMAINS.md` exists and contains `## Domain Enrichment (Optional)`: FOUND
- Commit `4a55dd7` exists: FOUND
